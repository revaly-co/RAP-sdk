# RAP Integration SDK — Open Items & Tracked Follow-Ups

**As of RFC-046 approval (2026-07-10, v10).** These are the items deliberately **not** decided by
the RFC — each is an implementation gate with an owner and a deadline. **Do not guess at these
during implementation**; they resolve through their owners and become ADRs (or ADR amendments)
when decided.

## Open questions (from RFC §11)

| # | Question | Owner | Gate / deadline | Notes |
| --- | --- | --- | --- | --- |
| **OQ-1** | Generator bake-off: openapi-generator vs commercial (Speakeasy / Fern / Stainless; Kiota for .NET) — one-page comparison, quality-per-language vs license cost | SC Eng | **Before build** (confirmed as a build gate at the approval walk) | Acceptance criterion = the DX contract §a idiomatic bar; a generator that can't meet it with template customization is disqualified. A commercial generator processes only the public API contract — no customer data leaves the org (RFC §9). **Bake-off evidence complete 2026-07-14** (`../generator-bakeoff.md`): hands-on generation of all six cores from `spec/v2.1.1+f5f9576` + verified July-2026 market research. Stainless exited the market 2026-05 (Anthropic acquisition). Byproducts: two platform spec defects found, both **fixed** — int64 `maximum` overflow (blocks .NET core compile; doc §3; Backbone PR #241 merged 2026-07-14 → `spec/v2.1.2+9af661b`) and the orphan `PaymentMethodRequest` schema (would ship as an unusable public type in all six SDKs; Backbone PR #242 merged 2026-07-15 → `spec/v2.1.3+e75c71a`, pinned in `../spec/pin.yaml`). ✅ **Decided 2026-07-14 → ADR-SDK-023**: OpenAPI Generator v7.23.0, digest-pinned, all six languages; no Kiota split; toolchain pin committed at `../pipeline/generator-pin.yaml`. **Gate closed — stage 2 has no remaining decision gates.** |
| **OQ-2** | Full machine-readable error-code taxonomy on `ErrorResponse.code` (beyond the D-7 safety binary) as a 2.1 refinement | SC Eng (API owners) | **Before Wave 2** | SDKs expose it in a **minor** release when it lands; `code` is parsed as an open string for exactly this reason (ADR-SDK-007). |
| **OQ-3** | Registry ownership/namespaces ×6 + protected publish environment | SC squad (accounts leadership-owned per ADR-SDK-011) | **Week 1** (confirmed at the approval walk) | Sequenced after the namespace decision (ADR-SDK-022, now decided → executable). Includes `security@` mailbox, 2FA/recovery, GPG keys in Key Vault, environment + tag protection per ADR-SDK-013. **Namespace status 2026-07-17:** org migration landed — the repo now lives at `revaly-co/RAP-sdk` (fleet-wide transfer; `FlexPay-io` parked with redirects, per ADR-SDK-022). Namespace gate satisfied. **Provisioning update 2026-07-17** (`../registry-provisioning.md`): namespace reservation underway on all six registries — npm/NuGet orgs and Maven Central `co.revaly` (DNS-verified) reserved org/namespace only; PyPI org application pending approval; Go binds to the GitHub namespace. **Deviation:** the Packagist vendor `revaly` was claimed via a temporary placeholder publish (`revaly/rap-sdk` v0.0.1, MIT, standalone `rap-sdk-php` repo), crossing the rule-3 embargo and ADR-SDK-019; accepted as a temporary hold (Charles, 2026-07-17), to be deleted and republished via the gated pipeline (Apache-2.0) at first real publish. **Naming decided 2026-07-30 → ADR-SDK-030**: npm **`@revaly/sdk`** (scoped) · PyPI `revaly-sdk` · NuGet `Revaly.Sdk`+`Revaly.Sdk.Core` · Packagist `revaly/sdk` · Maven `co.revaly:revaly-sdk` · Go path unchanged; committed metadata final in **all six** since 2026-08-03 (npm rename shipped with the stage-6 prep, ADR-SDK-031). **Environment update 2026-07-29:** GitHub environment `publish` created (per-language tag-pattern policies; `languages/go/v*` excluded until the Go ceremony); `security@` mailbox live + `SECURITY.md` merged 2026-07-30. **Prefix update 2026-08-03:** the NuGet `Revaly.*` ID-prefix is **RESERVED** — NuGet.org admin confirmed "reserved the prefix 'Revaly' for account 'revaly'" to the owner mailbox, one business day after the 2026-07-31 owner-mailbox resubmission. **Remaining under this OQ, exactly** (per-registry board: `../registry-provisioning.md`): (1) PyPI org approval — application pending in PyPI's queue (the only namespace act still open); (2) publish-day only, Legal-unblocked — the ADR-SDK-019 **written** ack was recorded 2026-08-06 (ADR § Ratification record); executed only via the flip runbook: OIDC trusted-publisher registrations npm/PyPI/NuGet (NuGet policies auto-expire after 7 unused days on private repos — create at publish), Maven GPG keys in Key Vault, Packagist webhook + placeholder deletion (the npm metadata rename shipped 2026-08-03 — ADR-SDK-031); (3) migration follow-up: re-provision `SPEC_ARTIFACT_READ_TOKEN` under the `revaly-co` resource owner (fine-grained PATs do not follow a repo across orgs — `pipeline/README.md`). **Maven signing key update 2026-08-06** (`../maven-signing-key.md`): the GPG keypair is **generated and vaulted** — RSA-4096 `07E856878E77A944169F56683ED2EB632E4EAAE7`, `packages@revaly.co`, expires 2028-08-05; four secrets in the publish Key Vault under leadership custody. **Deviation:** this is a flip-act-6 item executed ahead of the ADR-SDK-019 **written** gate; accepted by Charles 2026-08-06 on the Packagist-placeholder precedent and recorded in `../maven-signing-key.md` § Governance; the written gate then closed later the same day (recorded 2026-08-06), shrinking the deviation's head start to hours. It stays narrow — signing material is not a registry credential, no registry was contacted, no Central Portal token exists, the keyserver upload (the irreversible public act) is deferred, and the embargo guards plus `REGISTRY_PUBLISH_MODE` are untouched. **Still open on the Maven leg:** the workload identity + per-secret `Key Vault Secrets User` bindings and the `PUBLISH_AZURE_*`/`PUBLISH_KEYVAULT_NAME` variables (needs prod access — assign); `maven-central-token` (embargoed; the fetch step reads it unconditionally, so java live fails without it); the keyserver upload; and the javadoc-jar gap (code, no prod access). **Portal custody update 2026-08-06:** `co.revaly` is owned by a leadership Portal account on an individual mailbox, and a support request is in flight to add a second squad publisher — accepted interim deviation from ADR-SDK-011, recorded with its closure plan in `../registry-provisioning.md` § Known deviation: Maven Central Portal account custody. |
| **OQ-6** | Default connect/total deadlines — derive from RAP-core latency telemetry, don't invent | SC Eng | **Before Wave 1 GA** (confirmed as written) | ✅ **Decided 2026-07-20 → ADR-SDK-027**: overall-deadline default **75 s** in all six runtimes, ratified from production telemetry (two independent sources pulled 2026-07-20 — RAP-path 5-day per-gateway percentiles + a 14-day unsampled gateway-fleet window; the 30 s telemetry floor clips ~1 in 9,500 — the owner ratified 75 s as a safety margin clearing every observed non-ceiling tail (worst 64 s), clipping ≲0.007 %, 25 s under the platform's ≈100 s ceiling; full evidence in the internal OQ-6 record on SC-278). Per-language opt-out sentinels documented in the ADR. Residuals reassigned, not dropped: **connect default → OQ-11** (not derivable from server-side data; ships as a minor config-default change when edge data lands); **reconcile-policy defaults → SC-261 follow-up** (need post-charge visibility-lag telemetry, not charge latency). `runtime-tdd.md` §1 updated. |
| **OQ-11** | Verify AFD/WAF edge behaviour (502/504, resets) and confirm the taxonomy classification against reality | SC Eng + DevOps | **Hard pre-GA gate** (confirmed hard at the approval walk) | ✅ **Decided 2026-07-21 → ADR-SDK-029**: edge rows ratified **unchanged** against the live production edge (30-day census of `api.revaly.co`: **one** edge 5xx — a 504/OriginTimeout on a synthetic health probe, connect-phase at 4.0 s, origin provably never received it; zero 502s; zero AFD-generated 503s; the WAF blocks scanner traffic only, and its bot-management rules log rather than block server-side SDK clients; origin error bodies pass through unmodified → the P-1 `not_processed` signal survives the edge). Documented nuances: an edge 504 can fire at ~4 s (never implies the deadline elapsed); edge bodies are HTML → the open-string `code` fallthrough is load-bearing. **Connect default 10 s ×6** (TypeScript documentation-level per ADR-SDK-028 §4; the §4 constraint honoured, 10 < 75) with per-language opt-out sentinels in the ADR; PHP expiry stays OutcomeUnknown (errno-28 precedent). Timeout ordering ratified 75 s < ≈100 s < AFD 120 s, plus an ops guardrail (edge-timeout lowering / bot-management hardening / payment-path WAF rules must be flagged to SDK owners). Ships v0.4.0 ×6. The ADR-SDK-024 GA sandbox retarget now waits only on its ADR-SDK-014 parity leg. |

## Post-RFC open items

(Added after RFC approval — same discipline: an owner, a gate, an ADR when decided. Numbering
continues after the RFC's OQ-15.)

| # | Question | Owner | Gate / deadline | Notes |
| --- | --- | --- | --- | --- |
| **OQ-16** | Stage-4 contract-smoke environment & per-language live E2E: Backbone staging ACA (dev subs) interim → merchant sandbox **key-scope on the shared production URL** at GA | SC Eng + DevOps | **Stage-4 build story — after all six runtimes** (added 2026-07-17) | ✅ **Decided + built 2026-07-18 → ADR-SDK-024 Accepted** (implementation record in the ADR). Six `smoke-<language>` CI jobs chained after stage 3 (release tags blocking · nightly advisory · manual dispatch — never plain PRs) run the thin 8-scenario suites in `../languages/*/smoke/`, incl. the staging-only fault-injected `503+not_processed` fast-failover row. Secrets are environment-scoped (GitHub environment `staging`): `RAP_SMOKE_BASE_URL` / `RAP_SMOKE_API_KEY` / `RAP_SMOKE_GATEWAY_ROUTING_ID` — borrowed from the platform's staging E2E pool (scope lives on the key; staging always simulates); **provisioning those three secrets is the one open act**. Remaining under this item: the platform-repo reverse-dogfood job (ADR §Decision 3, advisory) and the GA sandbox retarget (ADR §Decision 4, gated on ADR-SDK-014 parity — its OQ-11 leg closed 2026-07-21, ADR-SDK-029). Earlier facts + the 2026-07-17 quickstart wording sweep stand as recorded in the ADR. **Decision-4 evidence landed 2026-07-25** (`../prod-sandbox-validation.md`): the released **v0.4.1** artifacts ×6, installed as a merchant installs them, were exercised against the **merchant sandbox key-scope on the shared production URL** through the real AFD/WAF edge — **6/6 green, 39 passed / 0 failed / 8 skipped**; harness lives in the separate repo **`revaly-co/RAP-sdk-integration-tests`** (released artifacts only, never `languages/*/` source). Every §2 row reachable there is proven ×6; the credential model (one URL, key scope selects the environment) is confirmed in practice. **Blocking finding for the retarget: the fault injector is inert on the production path** — the `503 + not_processed` fast-failover row cannot be a live-sandbox assertion, so dropping `RAP_SMOKE_FAULT_INJECT` (per ADR §Decision 4) must be paired with a written acceptance that the row moves to mock-only coverage (`runtime-tdd.md` §8). ADR-SDK-014 parity checklist after this run: taxonomy rows **partial** (all but `not_processed`), P-1 `code` emission **unobserved** (blocked by the same gap), BIN-routing **characterised** (routing is `gatewayRoutingId`-driven, so the retargeted job needs the token as a third secret), OQ-11 leg ✅ closed. **Retarget prepared 2026-07-25 — now a pure provisioning act, no workflow edit.** `pipeline.yml` sources `RAP_SMOKE_FAULT_INJECT` from the `staging` environment **variable** (set to `pre-dispatch` today, so staging behaviour is unchanged) and the fail-closed guard now also requires `RAP_SMOKE_GATEWAY_ROUTING_ID`. The coverage delta is **accepted in writing** in ADR-SDK-024 §"Decision-4 retarget" (the `503+not_processed` row moves to mock-only; compensating controls + revisit trigger recorded there), along with the three-step runbook. **Correction:** the `staging` environment and all three secrets **already exist** (earlier "provisioning those three secrets is the one open act" is stale) — they point at Backbone staging today, proven by the v0.4.1 release run passing stage 4 including the injector-only row. ✅ **Decision 4 EXECUTED 2026-07-25, then upgraded to a TWO-TARGET design the same day** — the coverage delta is **recovered, not accepted**. Each language job now runs its suite twice: step 1 against the prod sandbox key-scope (GA target, injector not passed → `not_processed` SKIPs; single-target proof run [30176984589](https://github.com/revaly-co/RAP-sdk/actions/runs/30176984589) was 6/6 green) and step 2 against Backbone staging with `RAP_SMOKE_FAULT_INJECT`, the only place the injector exists — so the full §2 taxonomy stays covered with nothing demoted to mock-only. Secrets split to `RAP_SANDBOX_*` + `RAP_SMOKE_*` (six total) with the injector variable **fail-closed** (unset = error, not a silent skip). Two riders: (a) the accompanying workflow PR must merge before the next stage-4 run from `main` (nightly 06:00 UTC / release tag), since `main` still hardcodes the injector header and would false-fail that row; (b) **tracked deviation** — the repoint used the already-held sandbox key rather than an Enablement-issued CI key (ADR-SDK-014), so the key's custody is wider than ADR-SDK-014 intends until the swap + rotate follow-up (recorded in the ADR) lands. Decision 3 (reverse-dogfood) additionally needs a cross-repo read credential — same family as OQ-17 / the `SPEC_ARTIFACT_READ_TOKEN` re-provisioning, since the platform workflow must reach this private repo's smoke suites. |
| **OQ-17** | ADR-SDK-025 **tier 2**: platform-dispatched draft re-pin PRs — `repository_dispatch` step in the platform's `spec-artifact.yml` + a receiving workflow here that opens the verified draft re-pin PR (pin bump + regen ×6) on `repin/spec-v<version>` | SC Eng + DevOps | **When the cross-repo credential lands** (Dimitri signals; same provisioning family as the OQ-3 `SPEC_ARTIFACT_READ_TOKEN` follow-up — prefer an org GitHub App over a long-lived PAT) | Added 2026-07-19 with ADR-SDK-025 (Accepted; tier 1 watchdog implemented — `spec-freshness.yml`, nightly 06:15 UTC). Tier 1 covers the gap meanwhile (≤24 h staleness bound) and demotes to backstop when this lands. |

## Tracked follow-ups (accepted at approval — RFC §12)

| Item | Owner | Gate | Notes |
| --- | --- | --- | --- |
| **PRD-057 deviation acceptance recorded** (typed-error + transient Musts) | Product | Follow-up to approval; verify before Wave-1 GA references the PRD | ADR-SDK-021. The 2026-07-10 approval was explicitly given ahead of this recording. |
| **§10 NFR self-audit walk by the SC squad** | SC squad (ADR-SDK-018) | Accepted follow-up; sizes P-2's hot-path write before GA | Platform repo ADR 014 carries the NFR gates (capacity/latency sizing + load test). |
| **Apache-2.0 Legal ratification** | Leadership + Legal | **Before first publish** (incl. pre-1.0) | ✅ **Ratified in writing — recorded 2026-08-06** (signed record in the internal RFC-046 record; supersedes the 2026-07-29 verbal). ADR-SDK-019 § Ratification record. |
| **QSA assessment / compliance policy link** (PCI scope) | Leadership + Compliance | When available | ADR-SDK-020 — the approval covers the determination meanwhile. |
| **Jerome's approval row** | Jerome Lecorbeiller | Optional (RFC is Approved via Charles) | Header approver list names both. |
| **Jira stories for P-1, P-2, spec fixes** | SC Eng | Sprint planning | Map to Epic SC-234 (platform repo ADRs 013/014/015 are the specs). |

## Platform-side dependencies the SDK waits on

(Details in the platform repo's ADRs — listed here so the SDK repo knows its external gates.)

| Platform work | SDK gate it unblocks | Status (2026-07-14) |
| --- | --- | --- |
| P-1 `ErrorResponse.code` (platform ADR 013) | **SDK V1** | ✅ **Done** — merged (platform PR #238) + deployed to production 2026-07-14 (release 29.6). **SDK V1 unblocked.** |
| P-2 intent reservation (platform ADR 014) | **SDK GA** (`SafeToFailover`) | ⏳ In progress — hot-path reservation write + duplicate suppression (409) live in production 2026-07-14 (29.6); 5-day production-load evidence recorded 2026-07-20 (zero reservation write failures, pg P99 7.1 ms; internal R13 record on SC-278 — sizing clause signed off at story level); pg_cron registration rollout gate + platform ADR-014 NFR walk still open (Epic SC-234) |
| Spec corrections: `format: int64`, id-length 100 (platform ADR 015) | First generator run | ✅ Merged 2026-07-14 (PR #238; ten Gate 3 findings per-finding allowlisted) |
| Gated spec artifact publication (platform ADR 016) | Pipeline stage 1 input | ✅ Live — first artifact 2026-07-14: `spec/v2.1.1+f5f9576`; current: `spec/v2.2.0+1e59620` (additive `statementDescriptor` on charge request/response, STR-108 / PR #228, 2026-07-16; prior: `v2.1.3+e75c71a` orphan-schema drop PR #242, `v2.1.2+9af661b` int64-`maximum` fix PR #241) — **pinned in `../spec/pin.yaml` 2026-07-16**, checksum + provenance verified |
| Spec-vs-implementation contract suite, 100% coverage (platform ADR 017) | Wave-1 GA discipline (D-6) | ✅ Merged 2026-07-14 (123-case L0 suite; PR Gate 2 + artifact precondition; `transactionType` widened to open string — generators must not assume a closed enum; spec-sync touch check retired, PR #240) |
| SDK `User-Agent` telemetry segmentation (platform ADR 018) | Adoption KPI + per-version dashboards from Wave 1 | ✅ Merged + in production 2026-07-14 (29.6; §8 dashboards + alert = IaC follow-up — unblocked 2026-07-20 by ADR-SDK-027 closing OQ-6) |
