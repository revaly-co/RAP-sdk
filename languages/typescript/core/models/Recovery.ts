/* tslint:disable */
/* eslint-disable */
/**
 * Revaly
 * Payment processing API for transaction and payment method management.  ## API Versioning  RAP supports an explicit, selectable API version so you can build against a stable, pinned contract while existing integrations keep working unchanged.  - **How to select a version:** send the `X-Api-Version` request header   (e.g. `X-Api-Version: 2.0`). The version lives in the header — request   URLs do not change. - **Default when omitted:** requests without the header (or with an   unrecognised header name) bind to the **base version `2.0`**, which is the   current contract. Existing integrations therefore continue unchanged. - **Unsupported versions:** a header naming a version that does not exist   returns **HTTP 400** with a structured error listing the supported   versions — a request is never silently bound to a different contract.   This includes an **empty or whitespace value**: if the `X-Api-Version`   header is present, it must name a supported version. Only a fully   absent header binds to the default. - **Supported versions** are advertised via the `api-supported-versions`   header on every response from the versioned API endpoints (payments,   payment methods, transactions, notify). Currently: `2.0`, `2.1`. - **Which version to use:** new integrations should pin **`2.1`**. It is   behaviourally identical to `2.0` today, and it is where future contract   refinements will land — pinning it now means you never migrate the   header. `2.0` is the frozen launch contract and remains the binding for   requests that send no version header.  ## Request tracing  Every API response — success and error alike — carries an `X-Correlation-ID` header. Send your own value (any non-empty string) and it is echoed back verbatim; omit it and the platform generates one. Quote the id when contacting support: it joins the request directly to platform telemetry. Treat it as an opaque string. 
 *
 * The version of the OpenAPI document: 2.6.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 * RAP SDK generated core — DO NOT EDIT (ADR-SDK-001; CI regeneration-diff enforced).
 * Regenerate only via pipeline/generate.sh: spec input pinned by spec/pin.yaml
 * (ADR-SDK-006), generator digest-pinned by pipeline/generator-pin.yaml (ADR-SDK-023).
 */

import { mapValues } from '../runtime';
/**
 * Recovery settings and customer recovery information
 * @export
 * @interface Recovery
 */
export interface Recovery {
    /**
     * Whether customer recovery is disabled for this transaction
     * @type {boolean}
     * @memberof Recovery
     */
    disableCustomerRecovery?: boolean | null;
    /**
     * Describes whether the approval should be attributed to the merchant for billing purposes
     * @type {boolean}
     * @memberof Recovery
     */
    externalApproval?: boolean | null;
    /**
     * Customer account number for recovery purposes
     * @type {string}
     * @memberof Recovery
     */
    customerAccountNumber?: string | null;
    /**
     * Customer account balance in smallest currency unit (e.g., cents for USD)
     * @type {number}
     * @memberof Recovery
     */
    customerBalance?: number | null;
    /**
     * Whether SMS notifications are disabled for recovery
     * @type {boolean}
     * @memberof Recovery
     */
    disableSMSNotification?: boolean | null;
    /**
     * Whether email notifications are disabled for recovery
     * @type {boolean}
     * @memberof Recovery
     */
    disableEmailNotification?: boolean | null;
    /**
     * Number of retry attempts for this billing cycle (initial attempt is 0)
     * @type {number}
     * @memberof Recovery
     */
    retryCount?: number | null;
    /**
     * Reference data received when a payment failed. This data should be returned on retry transactions for the same payment.
     * @type {string}
     * @memberof Recovery
     */
    paymentReferenceData?: string | null;
    /**
     * Date and time of the first transaction attempt for this billing cycle. Required when retry count is greater than 0.
     * @type {Date}
     * @memberof Recovery
     */
    dateFirstAttempt?: Date | null;
}

/**
 * Check if a given object implements the Recovery interface.
 */
export function instanceOfRecovery(value: object): value is Recovery {
    // RAP fork (pipeline/typescript/config.yaml): every property is optional, so a purely
    // structural check would accept ANY object and break oneOf discrimination — match only
    // when at least one declared property is present (names inspected, never values).
    return ('disableCustomerRecovery' in value && value['disableCustomerRecovery'] !== undefined)
        || ('externalApproval' in value && value['externalApproval'] !== undefined)
        || ('customerAccountNumber' in value && value['customerAccountNumber'] !== undefined)
        || ('customerBalance' in value && value['customerBalance'] !== undefined)
        || ('disableSMSNotification' in value && value['disableSMSNotification'] !== undefined)
        || ('disableEmailNotification' in value && value['disableEmailNotification'] !== undefined)
        || ('retryCount' in value && value['retryCount'] !== undefined)
        || ('paymentReferenceData' in value && value['paymentReferenceData'] !== undefined)
        || ('dateFirstAttempt' in value && value['dateFirstAttempt'] !== undefined);
}

export function RecoveryFromJSON(json: any): Recovery {
    return RecoveryFromJSONTyped(json, false);
}

export function RecoveryFromJSONTyped(json: any, ignoreDiscriminator: boolean): Recovery {
    if (json == null) {
        return json;
    }
    return {
        
        'disableCustomerRecovery': json['disableCustomerRecovery'] == null ? undefined : json['disableCustomerRecovery'],
        'externalApproval': json['externalApproval'] == null ? undefined : json['externalApproval'],
        'customerAccountNumber': json['customerAccountNumber'] == null ? undefined : json['customerAccountNumber'],
        'customerBalance': json['customerBalance'] == null ? undefined : json['customerBalance'],
        'disableSMSNotification': json['disableSMSNotification'] == null ? undefined : json['disableSMSNotification'],
        'disableEmailNotification': json['disableEmailNotification'] == null ? undefined : json['disableEmailNotification'],
        'retryCount': json['retryCount'] == null ? undefined : json['retryCount'],
        'paymentReferenceData': json['paymentReferenceData'] == null ? undefined : json['paymentReferenceData'],
        'dateFirstAttempt': json['dateFirstAttempt'] == null ? undefined : (new Date(json['dateFirstAttempt'])),
    };
}

export function RecoveryToJSON(json: any): Recovery {
    return RecoveryToJSONTyped(json, false);
}

export function RecoveryToJSONTyped(value?: Recovery | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'disableCustomerRecovery': value['disableCustomerRecovery'],
        'externalApproval': value['externalApproval'],
        'customerAccountNumber': value['customerAccountNumber'],
        'customerBalance': value['customerBalance'],
        'disableSMSNotification': value['disableSMSNotification'],
        'disableEmailNotification': value['disableEmailNotification'],
        'retryCount': value['retryCount'],
        'paymentReferenceData': value['paymentReferenceData'],
        'dateFirstAttempt': value['dateFirstAttempt'] == null ? value['dateFirstAttempt'] : value['dateFirstAttempt'].toISOString(),
    };
}

