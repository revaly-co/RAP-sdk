/* tslint:disable */
/* eslint-disable */
/**
 * Revaly
 * Payment processing API for transaction and payment method management.  ## API Versioning  RAP supports an explicit, selectable API version so you can build against a stable, pinned contract while existing integrations keep working unchanged.  - **How to select a version:** send the `X-Api-Version` request header   (e.g. `X-Api-Version: 2.0`). The version lives in the header — request   URLs do not change. - **Default when omitted:** requests without the header (or with an   unrecognised header name) bind to the **base version `2.0`**, which is the   current contract. Existing integrations therefore continue unchanged. - **Unsupported versions:** a header naming a version that does not exist   returns **HTTP 400** with a structured error listing the supported   versions — a request is never silently bound to a different contract.   This includes an **empty or whitespace value**: if the `X-Api-Version`   header is present, it must name a supported version. Only a fully   absent header binds to the default. - **Supported versions** are advertised via the `api-supported-versions`   header on every response from the versioned API endpoints (payments,   payment methods, transactions, notify). Currently: `2.0`, `2.1`. - **Which version to use:** new integrations should pin **`2.1`**. It is   behaviourally identical to `2.0` today, and it is where future contract   refinements will land — pinning it now means you never migrate the   header. `2.0` is the frozen launch contract and remains the binding for   requests that send no version header.  ## Request tracing  Every API response — success and error alike — carries an `X-Correlation-ID` header. Send your own value (any non-empty string) and it is echoed back verbatim; omit it and the platform generates one. Quote the id when contacting support: it joins the request directly to platform telemetry. Treat it as an opaque string. 
 *
 * The version of the OpenAPI document: 2.2.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 * RAP SDK generated core — DO NOT EDIT (ADR-SDK-001; CI regeneration-diff enforced).
 * Regenerate only via pipeline/generate.sh: spec input pinned by spec/pin.yaml
 * (ADR-SDK-006), generator digest-pinned by pipeline/generator-pin.yaml (ADR-SDK-023).
 */

import { mapValues } from '../runtime';
/**
 * 3D Secure authentication data for payment processing
 * @export
 * @interface ThreeDS
 */
export interface ThreeDS {
    /**
     * 3DS protocol version used for the auth result.
     * @type {string}
     * @memberof ThreeDS
     */
    version?: string | null;
    /**
     * Requested/declared SCA exemption routed to the gateway (e.g., 'moto', 'low_value').
     * @type {string}
     * @memberof ThreeDS
     */
    exemptionType?: string | null;
    /**
     * Electronic Commerce Indicator (scheme-specific). Visa: 05/06/07, MC: 02/01/00.
     * @type {string}
     * @memberof ThreeDS
     */
    eci?: string | null;
    /**
     * Cardholder Authentication Verification Value (CAVV/AVV/UCAF cryptogram). Base64 of a 20-byte value (28 chars).
     * @type {string}
     * @memberof ThreeDS
     */
    cryptogram?: string | null;
    /**
     * Directory Server transaction ID (UUID, 36 chars).
     * @type {string}
     * @memberof ThreeDS
     */
    dsTransactionId?: string | null;
    /**
     * ACS (issuer) transaction ID (UUID, 36 chars).
     * @type {string}
     * @memberof ThreeDS
     */
    acsTransactionId?: string | null;
    /**
     * Authentication transaction ID (legacy 3DS1, Base64 of 20 bytes, 28 chars).
     * @type {string}
     * @memberof ThreeDS
     */
    xid?: string | null;
    /**
     * Algorithm used to generate the cryptogram. Often '1'.
     * @type {string}
     * @memberof ThreeDS
     */
    cavvAlgorithm?: string | null;
    /**
     * Directory Server response status (TransStatus). Typical: Y, N, U, A, R.
     * @type {ThreeDSDirectoryStatusEnum}
     * @memberof ThreeDS
     */
    directoryStatus?: ThreeDSDirectoryStatusEnum | null;
    /**
     * Final cardholder authentication status from ACS/3DS Server (TransStatus). Typical: Y, N, U, A, R.
     * @type {ThreeDSAuthenticationStatusEnum}
     * @memberof ThreeDS
     */
    authenticationStatus?: ThreeDSAuthenticationStatusEnum | null;
    /**
     * Enrollment status (legacy 3DS1, Verify Enrollment Response). Typical: Y, N, U.
     * @type {ThreeDSEnrolledStatusEnum}
     * @memberof ThreeDS
     */
    enrolledStatus?: ThreeDSEnrolledStatusEnum | null;
    /**
     * 3DS Server transaction ID (UUID, 36 chars).
     * @type {string}
     * @memberof ThreeDS
     */
    serverTransId?: string | null;
}


/**
 * @export
 */
export const ThreeDSDirectoryStatusEnum = {
    Y: 'Y',
    N: 'N',
    U: 'U',
    A: 'A',
    R: 'R'
} as const;
export type ThreeDSDirectoryStatusEnum = typeof ThreeDSDirectoryStatusEnum[keyof typeof ThreeDSDirectoryStatusEnum];

/**
 * @export
 */
export const ThreeDSAuthenticationStatusEnum = {
    Y: 'Y',
    N: 'N',
    U: 'U',
    A: 'A',
    R: 'R'
} as const;
export type ThreeDSAuthenticationStatusEnum = typeof ThreeDSAuthenticationStatusEnum[keyof typeof ThreeDSAuthenticationStatusEnum];

/**
 * @export
 */
export const ThreeDSEnrolledStatusEnum = {
    Y: 'Y',
    N: 'N',
    U: 'U'
} as const;
export type ThreeDSEnrolledStatusEnum = typeof ThreeDSEnrolledStatusEnum[keyof typeof ThreeDSEnrolledStatusEnum];


/**
 * Check if a given object implements the ThreeDS interface.
 */
export function instanceOfThreeDS(value: object): value is ThreeDS {
    // RAP fork (pipeline/typescript/config.yaml): every property is optional, so a purely
    // structural check would accept ANY object and break oneOf discrimination — match only
    // when at least one declared property is present (names inspected, never values).
    return ('version' in value && value['version'] !== undefined)
        || ('exemptionType' in value && value['exemptionType'] !== undefined)
        || ('eci' in value && value['eci'] !== undefined)
        || ('cryptogram' in value && value['cryptogram'] !== undefined)
        || ('dsTransactionId' in value && value['dsTransactionId'] !== undefined)
        || ('acsTransactionId' in value && value['acsTransactionId'] !== undefined)
        || ('xid' in value && value['xid'] !== undefined)
        || ('cavvAlgorithm' in value && value['cavvAlgorithm'] !== undefined)
        || ('directoryStatus' in value && value['directoryStatus'] !== undefined)
        || ('authenticationStatus' in value && value['authenticationStatus'] !== undefined)
        || ('enrolledStatus' in value && value['enrolledStatus'] !== undefined)
        || ('serverTransId' in value && value['serverTransId'] !== undefined);
}

export function ThreeDSFromJSON(json: any): ThreeDS {
    return ThreeDSFromJSONTyped(json, false);
}

export function ThreeDSFromJSONTyped(json: any, ignoreDiscriminator: boolean): ThreeDS {
    if (json == null) {
        return json;
    }
    return {
        
        'version': json['version'] == null ? undefined : json['version'],
        'exemptionType': json['exemptionType'] == null ? undefined : json['exemptionType'],
        'eci': json['eci'] == null ? undefined : json['eci'],
        'cryptogram': json['cryptogram'] == null ? undefined : json['cryptogram'],
        'dsTransactionId': json['dsTransactionId'] == null ? undefined : json['dsTransactionId'],
        'acsTransactionId': json['acsTransactionId'] == null ? undefined : json['acsTransactionId'],
        'xid': json['xid'] == null ? undefined : json['xid'],
        'cavvAlgorithm': json['cavvAlgorithm'] == null ? undefined : json['cavvAlgorithm'],
        'directoryStatus': json['directoryStatus'] == null ? undefined : json['directoryStatus'],
        'authenticationStatus': json['authenticationStatus'] == null ? undefined : json['authenticationStatus'],
        'enrolledStatus': json['enrolledStatus'] == null ? undefined : json['enrolledStatus'],
        'serverTransId': json['serverTransId'] == null ? undefined : json['serverTransId'],
    };
}

export function ThreeDSToJSON(json: any): ThreeDS {
    return ThreeDSToJSONTyped(json, false);
}

export function ThreeDSToJSONTyped(value?: ThreeDS | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'version': value['version'],
        'exemptionType': value['exemptionType'],
        'eci': value['eci'],
        'cryptogram': value['cryptogram'],
        'dsTransactionId': value['dsTransactionId'],
        'acsTransactionId': value['acsTransactionId'],
        'xid': value['xid'],
        'cavvAlgorithm': value['cavvAlgorithm'],
        'directoryStatus': value['directoryStatus'],
        'authenticationStatus': value['authenticationStatus'],
        'enrolledStatus': value['enrolledStatus'],
        'serverTransId': value['serverTransId'],
    };
}

