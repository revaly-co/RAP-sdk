/* tslint:disable */
/* eslint-disable */
/**
 * Revaly
 * Payment processing API for transaction and payment method management.  ## API Versioning  RAP supports an explicit, selectable API version so you can build against a stable, pinned contract while existing integrations keep working unchanged.  - **How to select a version:** send the `X-Api-Version` request header   (e.g. `X-Api-Version: 2.0`). The version lives in the header — request   URLs do not change. - **Default when omitted:** requests without the header (or with an   unrecognised header name) bind to the **base version `2.0`**, which is the   current contract. Existing integrations therefore continue unchanged. - **Unsupported versions:** a header naming a version that does not exist   returns **HTTP 400** with a structured error listing the supported   versions — a request is never silently bound to a different contract.   This includes an **empty or whitespace value**: if the `X-Api-Version`   header is present, it must name a supported version. Only a fully   absent header binds to the default. - **Supported versions** are advertised via the `api-supported-versions`   header on every response from the versioned API endpoints (payments,   payment methods, transactions, notify). Currently: `2.0`, `2.1`. - **Which version to use:** new integrations should pin **`2.1`**. It is   behaviourally identical to `2.0` today, and it is where future contract   refinements will land — pinning it now means you never migrate the   header. `2.0` is the frozen launch contract and remains the binding for   requests that send no version header.  ## Request tracing  Every API response — success and error alike — carries an `X-Correlation-ID` header. Send your own value (any non-empty string) and it is echoed back verbatim; omit it and the platform generates one. Quote the id when contacting support: it joins the request directly to platform telemetry. Treat it as an opaque string. 
 *
 * The version of the OpenAPI document: 2.3.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 * RAP SDK generated core — DO NOT EDIT (ADR-SDK-001; CI regeneration-diff enforced).
 * Regenerate only via pipeline/generate.sh: spec input pinned by spec/pin.yaml
 * (ADR-SDK-006), generator digest-pinned by pipeline/generator-pin.yaml (ADR-SDK-023).
 */

import { mapValues } from '../runtime';
/**
 * Vault-issued payment token details for payment processing
 * @export
 * @interface VaultPaymentMethod
 */
export interface VaultPaymentMethod {
    /**
     * Vault-issued token (any provider) used to authorize a payment.
     * Valid only on `/payments/charge` and `/payments/authorize` when `paymentMethodType` is `vaultToken`.
     * Requires `paymentMethod.merchantAccountReferenceId` for gateway routing. Must not be combined with `creditCard` or `gatewayPaymentMethod`.
     * 
     * @type {string}
     * @memberof VaultPaymentMethod
     */
    vaultToken?: string | null;
    /**
     * Bank Identification Number (first 6 or 8 digits)
     * @type {string}
     * @memberof VaultPaymentMethod
     */
    bin?: string | null;
    /**
     * Last four digits of the payment method
     * @type {string}
     * @memberof VaultPaymentMethod
     */
    lastFourDigits?: string | null;
    /**
     * Expiration year (YYYY)
     * @type {string}
     * @memberof VaultPaymentMethod
     */
    expiryYear?: string | null;
    /**
     * Expiration month (01-12)
     * @type {string}
     * @memberof VaultPaymentMethod
     */
    expiryMonth?: string | null;
}

/**
 * Check if a given object implements the VaultPaymentMethod interface.
 */
export function instanceOfVaultPaymentMethod(value: object): value is VaultPaymentMethod {
    // RAP fork (pipeline/typescript/config.yaml): every property is optional, so a purely
    // structural check would accept ANY object and break oneOf discrimination — match only
    // when at least one declared property is present (names inspected, never values).
    return ('vaultToken' in value && value['vaultToken'] !== undefined)
        || ('bin' in value && value['bin'] !== undefined)
        || ('lastFourDigits' in value && value['lastFourDigits'] !== undefined)
        || ('expiryYear' in value && value['expiryYear'] !== undefined)
        || ('expiryMonth' in value && value['expiryMonth'] !== undefined);
}

export function VaultPaymentMethodFromJSON(json: any): VaultPaymentMethod {
    return VaultPaymentMethodFromJSONTyped(json, false);
}

export function VaultPaymentMethodFromJSONTyped(json: any, ignoreDiscriminator: boolean): VaultPaymentMethod {
    if (json == null) {
        return json;
    }
    return {
        
        'vaultToken': json['vaultToken'] == null ? undefined : json['vaultToken'],
        'bin': json['bin'] == null ? undefined : json['bin'],
        'lastFourDigits': json['lastFourDigits'] == null ? undefined : json['lastFourDigits'],
        'expiryYear': json['expiryYear'] == null ? undefined : json['expiryYear'],
        'expiryMonth': json['expiryMonth'] == null ? undefined : json['expiryMonth'],
    };
}

export function VaultPaymentMethodToJSON(json: any): VaultPaymentMethod {
    return VaultPaymentMethodToJSONTyped(json, false);
}

export function VaultPaymentMethodToJSONTyped(value?: VaultPaymentMethod | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'vaultToken': value['vaultToken'],
        'bin': value['bin'],
        'lastFourDigits': value['lastFourDigits'],
        'expiryYear': value['expiryYear'],
        'expiryMonth': value['expiryMonth'],
    };
}

