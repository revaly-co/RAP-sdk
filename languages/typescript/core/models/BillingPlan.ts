/* tslint:disable */
/* eslint-disable */
/**
 * Revaly
 * Payment processing API for transaction and payment method management.  ## API Versioning  RAP supports an explicit, selectable API version so you can build against a stable, pinned contract while existing integrations keep working unchanged.  - **How to select a version:** send the `X-Api-Version` request header   (e.g. `X-Api-Version: 2.0`). The version lives in the header — request   URLs do not change. - **Default when omitted:** requests without the header (or with an   unrecognised header name) bind to the **base version `2.0`**, which is the   current contract. Existing integrations therefore continue unchanged. - **Unsupported versions:** a header naming a version that does not exist   returns **HTTP 400** with a structured error listing the supported   versions — a request is never silently bound to a different contract.   This includes an **empty or whitespace value**: if the `X-Api-Version`   header is present, it must name a supported version. Only a fully   absent header binds to the default. - **Supported versions** are advertised via the `api-supported-versions`   header on every response from the versioned API endpoints (payments,   payment methods, transactions, notify). Currently: `2.0`, `2.1`. - **Which version to use:** new integrations should pin **`2.1`**. It is   behaviourally identical to `2.0` today, and it is where future contract   refinements will land — pinning it now means you never migrate the   header. `2.0` is the frozen launch contract and remains the binding for   requests that send no version header.  ## Request tracing  Every API response — success and error alike — carries an `X-Correlation-ID` header. Send your own value (any non-empty string) and it is echoed back verbatim; omit it and the platform generates one. Quote the id when contacting support: it joins the request directly to platform telemetry. Treat it as an opaque string. 
 *
 * The version of the OpenAPI document: 2.6.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 * RAP SDK generated core — DO NOT EDIT (ADR-SDK-001; CI regeneration-diff enforced).
 * Regenerate only via pipeline/generate.sh: spec input pinned by spec/pin.yaml
 * (ADR-SDK-006), generator digest-pinned by pipeline/generator-pin.yaml (ADR-SDK-023).
 */


/**
 * Billing plan frequency and type for the subscription or payment model.
 * 
 * Available billing plans:
 * - **trial**: Free or limited introductory period (variable interval, days/weeks)
 * - **weekly**: Billed every week (1 week interval)
 * - **twicePerWeek**: Billed twice per week (3-4 days interval) - User is billed 2x per week
 * - **biWeekly**: Billed every 2 weeks (different than twice per month)
 * - **biMonthly**: Billed twice per month (2x per month)
 * - **monthly**: Standard monthly billing (1 month interval) - Standard SaaS cadence
 * - **quarterly**: Billed every 3 months - Often used for small business or agency plans
 * - **semiAnnual**: Billed every 6 months - Common prepaid option
 * - **annual**: Billed yearly (12 months interval) - Discounted yearly plan
 * - **biennial**: Billed every 2 years (24 months) - Long-term or enterprise contracts
 * - **triennial**: Billed every 3 years (36 months) - Multi-year hosting or enterprise terms
 * - **usageBased**: Pay-as-you-go or metered billing - Billed per usage or seat count each cycle (varies)
 * - **lifetime**: One-time payment with no renewal required
 * - **custom**: Negotiated or legacy plans with custom terms (variable)
 * - **other**: Fallback for undefined, experimental, or unspecified billing plans
 * 
 * @export
 */
export const BillingPlan = {
    Trial: 'trial',
    Weekly: 'weekly',
    TwicePerWeek: 'twicePerWeek',
    BiWeekly: 'biWeekly',
    BiMonthly: 'biMonthly',
    Monthly: 'monthly',
    Quarterly: 'quarterly',
    SemiAnnual: 'semiAnnual',
    Annual: 'annual',
    Biennial: 'biennial',
    Triennial: 'triennial',
    UsageBased: 'usageBased',
    Lifetime: 'lifetime',
    Custom: 'custom',
    Other: 'other'
} as const;
export type BillingPlan = typeof BillingPlan[keyof typeof BillingPlan];


export function instanceOfBillingPlan(value: any): boolean {
    for (const key in BillingPlan) {
        if (Object.prototype.hasOwnProperty.call(BillingPlan, key)) {
            if (BillingPlan[key as keyof typeof BillingPlan] === value) {
                return true;
            }
        }
    }
    return false;
}

export function BillingPlanFromJSON(json: any): BillingPlan {
    return BillingPlanFromJSONTyped(json, false);
}

export function BillingPlanFromJSONTyped(json: any, ignoreDiscriminator: boolean): BillingPlan {
    return json as BillingPlan;
}

export function BillingPlanToJSON(value?: BillingPlan | null): any {
    return value as any;
}

export function BillingPlanToJSONTyped(value: any, ignoreDiscriminator: boolean): BillingPlan {
    return value as BillingPlan;
}

