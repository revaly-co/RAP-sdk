/* tslint:disable */
/* eslint-disable */
/**
 * Revaly
 * Payment processing API for transaction and payment method management.  ## API Versioning  RAP supports an explicit, selectable API version so you can build against a stable, pinned contract while existing integrations keep working unchanged.  - **How to select a version:** send the `X-Api-Version` request header   (e.g. `X-Api-Version: 2.0`). The version lives in the header — request   URLs do not change. - **Default when omitted:** requests without the header (or with an   unrecognised header name) bind to the **base version `2.0`**, which is the   current contract. Existing integrations therefore continue unchanged. - **Unsupported versions:** a header naming a version that does not exist   returns **HTTP 400** with a structured error listing the supported   versions — a request is never silently bound to a different contract.   This includes an **empty or whitespace value**: if the `X-Api-Version`   header is present, it must name a supported version. Only a fully   absent header binds to the default. - **Supported versions** are advertised via the `api-supported-versions`   header on every response from the versioned API endpoints (payments,   payment methods, transactions, notify). Currently: `2.0`, `2.1`. - **Which version to use:** new integrations should pin **`2.1`**. It is   behaviourally identical to `2.0` today, and it is where future contract   refinements will land — pinning it now means you never migrate the   header. `2.0` is the frozen launch contract and remains the binding for   requests that send no version header.  ## Request tracing  Every API response — success and error alike — carries an `X-Correlation-ID` header. Send your own value (any non-empty string) and it is echoed back verbatim; omit it and the platform generates one. Quote the id when contacting support: it joins the request directly to platform telemetry. Treat it as an opaque string. 
 *
 * The version of the OpenAPI document: 2.6.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 * RAP SDK generated core — DO NOT EDIT (ADR-SDK-001; CI regeneration-diff enforced).
 * Regenerate only via pipeline/generate.sh: spec input pinned by spec/pin.yaml
 * (ADR-SDK-006), generator digest-pinned by pipeline/generator-pin.yaml (ADR-SDK-023).
 */

import { mapValues } from '../runtime';
import type { TransactionResponse } from './TransactionResponse';
import {
    TransactionResponseFromJSON,
    TransactionResponseFromJSONTyped,
    TransactionResponseToJSON,
    TransactionResponseToJSONTyped,
} from './TransactionResponse';

/**
 * Envelope returned by the V2 transaction-details lookups when `includeAllTransactions=true`.
 * Contains the matched transaction plus every transaction belonging to the same payment — all
 * attempts and lifecycle operations (capture, refund, void) that share the same initial
 * transaction id. If the matched transaction has no initial transaction id, `transactions`
 * contains only the matched record.
 * 
 * The required `transactions` member is always present and discriminates this envelope from
 * a single `TransactionResponse`, which never carries a `transactions` member.
 * 
 * @export
 * @interface TransactionGroupResponse
 */
export interface TransactionGroupResponse {
    /**
     * The transaction matching the supplied id (the record the non-expanded lookup returns).
     * @type {TransactionResponse}
     * @memberof TransactionGroupResponse
     */
    transaction?: TransactionResponse;
    /**
     * Every transaction in the payment, ordered by transaction date ascending. Capped at 100.
     * @type {Array<TransactionResponse>}
     * @memberof TransactionGroupResponse
     */
    transactions: Array<TransactionResponse>;
}

/**
 * Check if a given object implements the TransactionGroupResponse interface.
 */
export function instanceOfTransactionGroupResponse(value: object): value is TransactionGroupResponse {
    if (!('transactions' in value) || value['transactions'] === undefined) return false;
    return true;
}

export function TransactionGroupResponseFromJSON(json: any): TransactionGroupResponse {
    return TransactionGroupResponseFromJSONTyped(json, false);
}

export function TransactionGroupResponseFromJSONTyped(json: any, ignoreDiscriminator: boolean): TransactionGroupResponse {
    if (json == null) {
        return json;
    }
    return {
        
        'transaction': json['transaction'] == null ? undefined : TransactionResponseFromJSON(json['transaction']),
        'transactions': ((json['transactions'] as Array<any>).map(TransactionResponseFromJSON)),
    };
}

export function TransactionGroupResponseToJSON(json: any): TransactionGroupResponse {
    return TransactionGroupResponseToJSONTyped(json, false);
}

export function TransactionGroupResponseToJSONTyped(value?: TransactionGroupResponse | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'transaction': TransactionResponseToJSON(value['transaction']),
        'transactions': ((value['transactions'] as Array<any>).map(TransactionResponseToJSON)),
    };
}

