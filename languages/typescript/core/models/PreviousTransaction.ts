/* tslint:disable */
/* eslint-disable */
/**
 * Revaly
 * Payment processing API for transaction and payment method management.  ## API Versioning  RAP supports an explicit, selectable API version so you can build against a stable, pinned contract while existing integrations keep working unchanged.  - **How to select a version:** send the `X-Api-Version` request header   (e.g. `X-Api-Version: 2.0`). The version lives in the header — request   URLs do not change. - **Default when omitted:** requests without the header (or with an   unrecognised header name) bind to the **base version `2.0`**, which is the   current contract. Existing integrations therefore continue unchanged. - **Unsupported versions:** a header naming a version that does not exist   returns **HTTP 400** with a structured error listing the supported   versions — a request is never silently bound to a different contract.   This includes an **empty or whitespace value**: if the `X-Api-Version`   header is present, it must name a supported version. Only a fully   absent header binds to the default. - **Supported versions** are advertised via the `api-supported-versions`   header on every response from the versioned API endpoints (payments,   payment methods, transactions, notify). Currently: `2.0`, `2.1`. - **Which version to use:** new integrations should pin **`2.1`**. It is   behaviourally identical to `2.0` today, and it is where future contract   refinements will land — pinning it now means you never migrate the   header. `2.0` is the frozen launch contract and remains the binding for   requests that send no version header.  ## Request tracing  Every API response — success and error alike — carries an `X-Correlation-ID` header. Send your own value (any non-empty string) and it is echoed back verbatim; omit it and the platform generates one. Quote the id when contacting support: it joins the request directly to platform telemetry. Treat it as an opaque string. 
 *
 * The version of the OpenAPI document: 2.2.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 * RAP SDK generated core — DO NOT EDIT (ADR-SDK-001; CI regeneration-diff enforced).
 * Regenerate only via pipeline/generate.sh: spec input pinned by spec/pin.yaml
 * (ADR-SDK-006), generator digest-pinned by pipeline/generator-pin.yaml (ADR-SDK-023).
 */

import { mapValues } from '../runtime';
/**
 * Information about a previous transaction for reference
 * @export
 * @interface PreviousTransaction
 */
export interface PreviousTransaction {
    /**
     * Date of the previous transaction
     * @type {Date}
     * @memberof PreviousTransaction
     */
    transactionDate?: Date | null;
    /**
     * Merchant account reference ID from the previous transaction
     * @type {string}
     * @memberof PreviousTransaction
     */
    merchantAccountReferenceId?: string | null;
    /**
     * Gateway response code from the previous transaction
     * @type {string}
     * @memberof PreviousTransaction
     */
    gatewayCode?: string | null;
    /**
     * Gateway response message from the previous transaction
     * @type {string}
     * @memberof PreviousTransaction
     */
    gatewayMessage?: string | null;
    /**
     * Gateway message key from the previous transaction
     * @type {string}
     * @memberof PreviousTransaction
     */
    gatewayMessageKey?: string | null;
    /**
     * Previous status of the transaction (1 = Approved, 2 = Declined, 3 = Error)
     * @type {number}
     * @memberof PreviousTransaction
     */
    transactionStatus?: number | null;
    /**
     * AVS code from the previous transaction
     * @type {string}
     * @memberof PreviousTransaction
     */
    avsCode?: string | null;
    /**
     * AVS message from the previous transaction
     * @type {string}
     * @memberof PreviousTransaction
     */
    avsMessage?: string | null;
    /**
     * CVV code from the previous transaction
     * @type {string}
     * @memberof PreviousTransaction
     */
    cvvCode?: string | null;
    /**
     * CVV message from the previous transaction
     * @type {string}
     * @memberof PreviousTransaction
     */
    cvvMessage?: string | null;
}

/**
 * Check if a given object implements the PreviousTransaction interface.
 */
export function instanceOfPreviousTransaction(value: object): value is PreviousTransaction {
    // RAP fork (pipeline/typescript/config.yaml): every property is optional, so a purely
    // structural check would accept ANY object and break oneOf discrimination — match only
    // when at least one declared property is present (names inspected, never values).
    return ('transactionDate' in value && value['transactionDate'] !== undefined)
        || ('merchantAccountReferenceId' in value && value['merchantAccountReferenceId'] !== undefined)
        || ('gatewayCode' in value && value['gatewayCode'] !== undefined)
        || ('gatewayMessage' in value && value['gatewayMessage'] !== undefined)
        || ('gatewayMessageKey' in value && value['gatewayMessageKey'] !== undefined)
        || ('transactionStatus' in value && value['transactionStatus'] !== undefined)
        || ('avsCode' in value && value['avsCode'] !== undefined)
        || ('avsMessage' in value && value['avsMessage'] !== undefined)
        || ('cvvCode' in value && value['cvvCode'] !== undefined)
        || ('cvvMessage' in value && value['cvvMessage'] !== undefined);
}

export function PreviousTransactionFromJSON(json: any): PreviousTransaction {
    return PreviousTransactionFromJSONTyped(json, false);
}

export function PreviousTransactionFromJSONTyped(json: any, ignoreDiscriminator: boolean): PreviousTransaction {
    if (json == null) {
        return json;
    }
    return {
        
        'transactionDate': json['transactionDate'] == null ? undefined : (new Date(json['transactionDate'])),
        'merchantAccountReferenceId': json['merchantAccountReferenceId'] == null ? undefined : json['merchantAccountReferenceId'],
        'gatewayCode': json['gatewayCode'] == null ? undefined : json['gatewayCode'],
        'gatewayMessage': json['gatewayMessage'] == null ? undefined : json['gatewayMessage'],
        'gatewayMessageKey': json['gatewayMessageKey'] == null ? undefined : json['gatewayMessageKey'],
        'transactionStatus': json['transactionStatus'] == null ? undefined : json['transactionStatus'],
        'avsCode': json['avsCode'] == null ? undefined : json['avsCode'],
        'avsMessage': json['avsMessage'] == null ? undefined : json['avsMessage'],
        'cvvCode': json['cvvCode'] == null ? undefined : json['cvvCode'],
        'cvvMessage': json['cvvMessage'] == null ? undefined : json['cvvMessage'],
    };
}

export function PreviousTransactionToJSON(json: any): PreviousTransaction {
    return PreviousTransactionToJSONTyped(json, false);
}

export function PreviousTransactionToJSONTyped(value?: PreviousTransaction | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'transactionDate': value['transactionDate'] == null ? value['transactionDate'] : value['transactionDate'].toISOString(),
        'merchantAccountReferenceId': value['merchantAccountReferenceId'],
        'gatewayCode': value['gatewayCode'],
        'gatewayMessage': value['gatewayMessage'],
        'gatewayMessageKey': value['gatewayMessageKey'],
        'transactionStatus': value['transactionStatus'],
        'avsCode': value['avsCode'],
        'avsMessage': value['avsMessage'],
        'cvvCode': value['cvvCode'],
        'cvvMessage': value['cvvMessage'],
    };
}

