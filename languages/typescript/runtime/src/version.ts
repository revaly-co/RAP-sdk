/**
 * The package semver, no `v` prefix. Stage 5 stamps release versions at packaging time;
 * development and test runs carry the placeholder (kept in sync with package.json).
 */
export const SDK_VERSION = '0.0.0';
