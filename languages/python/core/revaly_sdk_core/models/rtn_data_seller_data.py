# coding: utf-8

"""
    Revaly

    Payment processing API for transaction and payment method management.  ## API Versioning  RAP supports an explicit, selectable API version so you can build against a stable, pinned contract while existing integrations keep working unchanged.  - **How to select a version:** send the `X-Api-Version` request header   (e.g. `X-Api-Version: 2.0`). The version lives in the header — request   URLs do not change. - **Default when omitted:** requests without the header (or with an   unrecognised header name) bind to the **base version `2.0`**, which is the   current contract. Existing integrations therefore continue unchanged. - **Unsupported versions:** a header naming a version that does not exist   returns **HTTP 400** with a structured error listing the supported   versions — a request is never silently bound to a different contract.   This includes an **empty or whitespace value**: if the `X-Api-Version`   header is present, it must name a supported version. Only a fully   absent header binds to the default. - **Supported versions** are advertised via the `api-supported-versions`   header on every response from the versioned API endpoints (payments,   payment methods, transactions, notify). Currently: `2.0`, `2.1`. - **Which version to use:** new integrations should pin **`2.1`**. It is   behaviourally identical to `2.0` today, and it is where future contract   refinements will land — pinning it now means you never migrate the   header. `2.0` is the frozen launch contract and remains the binding for   requests that send no version header.  ## Request tracing  Every API response — success and error alike — carries an `X-Correlation-ID` header. Send your own value (any non-empty string) and it is echoed back verbatim; omit it and the platform generates one. Quote the id when contacting support: it joins the request directly to platform telemetry. Treat it as an opaque string. 

    The version of the OpenAPI document: 2.2.1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.

    RAP SDK generated core — DO NOT EDIT (ADR-SDK-001; CI regeneration-diff enforced).
    Regenerate only via pipeline/generate.sh: spec input pinned by spec/pin.yaml
    (ADR-SDK-006), generator digest-pinned by pipeline/generator-pin.yaml (ADR-SDK-023).
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class RtnDataSellerData(BaseModel):
    """
    Seller details for marketplace or platform transactions where the client submits on behalf of a third-party seller (maps to Amex seller_information). All fields optional; omit rather than send nulls.
    """ # noqa: E501
    seller_id: Optional[Annotated[str, Field(strict=True, max_length=30)]] = Field(default=None, description="The client's identifier for the seller.", alias="sellerId", json_schema_extra={"examples": ["1234567890"]})
    seller_business_name: Optional[Annotated[str, Field(strict=True, max_length=30)]] = Field(default=None, description="Business name of the seller.", alias="sellerBusinessName", json_schema_extra={"examples": ["ACME Marketplace Vendor"]})
    seller_owner_name: Optional[Annotated[str, Field(strict=True, max_length=50)]] = Field(default=None, description="Name of the seller or the seller's owner.", alias="sellerOwnerName", json_schema_extra={"examples": ["Betty Smith"]})
    seller_tenure: Optional[StrictInt] = Field(default=None, description="Number of months the seller has used the client's services.", alias="sellerTenure", json_schema_extra={"examples": [36]})
    seller_latitude: Optional[StrictStr] = Field(default=None, description="Latitude (decimal degrees) where the purchase was made — seller location, not the cardholder device.", alias="sellerLatitude", json_schema_extra={"examples": ["-1.5459487"]})
    seller_longitude: Optional[StrictStr] = Field(default=None, description="Longitude (decimal degrees) where the purchase was made — seller location.", alias="sellerLongitude", json_schema_extra={"examples": ["52.2768309"]})
    seller_address: Optional[Annotated[str, Field(strict=True, max_length=50)]] = Field(default=None, description="Business or contact address of the seller.", alias="sellerAddress", json_schema_extra={"examples": ["400 Maple Court"]})
    seller_phone: Optional[Annotated[str, Field(strict=True, max_length=10)]] = Field(default=None, description="Primary phone number of the seller.", alias="sellerPhone", json_schema_extra={"examples": ["5555552222"]})
    seller_email: Optional[Annotated[str, Field(strict=True, max_length=60)]] = Field(default=None, description="Primary email address of the seller.", alias="sellerEmail", json_schema_extra={"examples": ["seller@example.com"]})
    seller_postal_code: Optional[Annotated[str, Field(strict=True, max_length=9)]] = Field(default=None, description="Postal code of the seller's primary address.", alias="sellerPostalCode", json_schema_extra={"examples": ["12345"]})
    seller_region: Optional[StrictStr] = Field(default=None, description="Seller region. APA=Asia Pacific & Australia; EMEA=Europe/Middle East/Africa; LA/C=Latin America & Caribbean.", alias="sellerRegion", json_schema_extra={"examples": ["USA"]})
    seller_country_code: Optional[StrictStr] = Field(default=None, description="Country of the seller, ISO 3166-1 numeric code as a string.", alias="sellerCountryCode", json_schema_extra={"examples": ["840"]})
    transaction_type_indicator: Optional[StrictStr] = Field(default=None, description="Seller transaction classification. P2P=Person to Person; P2M=Person to Merchant; CSH=Cash.", alias="transactionTypeIndicator", json_schema_extra={"examples": ["P2M"]})
    __properties: ClassVar[List[str]] = ["sellerId", "sellerBusinessName", "sellerOwnerName", "sellerTenure", "sellerLatitude", "sellerLongitude", "sellerAddress", "sellerPhone", "sellerEmail", "sellerPostalCode", "sellerRegion", "sellerCountryCode", "transactionTypeIndicator"]

    @field_validator('seller_region')
    def seller_region_validate_enum(cls, value):
        """Open vocabulary (RAP ADR-SDK-023 override): accept unknown enum values.

        The spec's enums are open vocabularies (ADR-SDK-007/010) — new server-side
        values must never break deserialization on success paths, so unknown wire
        values pass through verbatim instead of raising ValueError.
        """
        return value

    @field_validator('transaction_type_indicator')
    def transaction_type_indicator_validate_enum(cls, value):
        """Open vocabulary (RAP ADR-SDK-023 override): accept unknown enum values.

        The spec's enums are open vocabularies (ADR-SDK-007/010) — new server-side
        values must never break deserialization on success paths, so unknown wire
        values pass through verbatim instead of raising ValueError.
        """
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RtnDataSellerData from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RtnDataSellerData from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "sellerId": obj.get("sellerId"),
            "sellerBusinessName": obj.get("sellerBusinessName"),
            "sellerOwnerName": obj.get("sellerOwnerName"),
            "sellerTenure": obj.get("sellerTenure"),
            "sellerLatitude": obj.get("sellerLatitude"),
            "sellerLongitude": obj.get("sellerLongitude"),
            "sellerAddress": obj.get("sellerAddress"),
            "sellerPhone": obj.get("sellerPhone"),
            "sellerEmail": obj.get("sellerEmail"),
            "sellerPostalCode": obj.get("sellerPostalCode"),
            "sellerRegion": obj.get("sellerRegion"),
            "sellerCountryCode": obj.get("sellerCountryCode"),
            "transactionTypeIndicator": obj.get("transactionTypeIndicator")
        })
        return _obj


