# coding: utf-8

"""
    Revaly

    Payment processing API for transaction and payment method management.  ## API Versioning  RAP supports an explicit, selectable API version so you can build against a stable, pinned contract while existing integrations keep working unchanged.  - **How to select a version:** send the `X-Api-Version` request header   (e.g. `X-Api-Version: 2.0`). The version lives in the header — request   URLs do not change. - **Default when omitted:** requests without the header (or with an   unrecognised header name) bind to the **base version `2.0`**, which is the   current contract. Existing integrations therefore continue unchanged. - **Unsupported versions:** a header naming a version that does not exist   returns **HTTP 400** with a structured error listing the supported   versions — a request is never silently bound to a different contract.   This includes an **empty or whitespace value**: if the `X-Api-Version`   header is present, it must name a supported version. Only a fully   absent header binds to the default. - **Supported versions** are advertised via the `api-supported-versions`   header on every response from the versioned API endpoints (payments,   payment methods, transactions, notify). Currently: `2.0`, `2.1`. - **Which version to use:** new integrations should pin **`2.1`**. It is   behaviourally identical to `2.0` today, and it is where future contract   refinements will land — pinning it now means you never migrate the   header. `2.0` is the frozen launch contract and remains the binding for   requests that send no version header.  ## Request tracing  Every API response — success and error alike — carries an `X-Correlation-ID` header. Send your own value (any non-empty string) and it is echoed back verbatim; omit it and the platform generates one. Quote the id when contacting support: it joins the request directly to platform telemetry. Treat it as an opaque string. 

    The version of the OpenAPI document: 2.6.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.

    RAP SDK generated core — DO NOT EDIT (ADR-SDK-001; CI regeneration-diff enforced).
    Regenerate only via pipeline/generate.sh: spec input pinned by spec/pin.yaml
    (ADR-SDK-006), generator digest-pinned by pipeline/generator-pin.yaml (ADR-SDK-023).
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class RtnDataCustomerData(BaseModel):
    """
    Customer account and profile signals. All fields optional.
    """ # noqa: E501
    first_name: Optional[Annotated[str, Field(strict=True, max_length=45)]] = Field(default=None, description="Customer first name.", alias="firstName", json_schema_extra={"examples": ["John"]})
    last_name: Optional[Annotated[str, Field(strict=True, max_length=45)]] = Field(default=None, description="Customer last name.", alias="lastName", json_schema_extra={"examples": ["Doe"]})
    email: Optional[Annotated[str, Field(strict=True, max_length=30)]] = Field(default=None, description="Customer email address.", json_schema_extra={"examples": ["john.doe@example.com"]})
    home_phone: Optional[Annotated[str, Field(strict=True, max_length=18)]] = Field(default=None, description="Home phone number. Digits only, no formatting.", alias="homePhone", json_schema_extra={"examples": ["5145550100"]})
    mobile_phone: Optional[Annotated[str, Field(strict=True, max_length=18)]] = Field(default=None, description="Mobile phone number. Digits only, no formatting.", alias="mobilePhone", json_schema_extra={"examples": ["5145550199"]})
    work_phone: Optional[Annotated[str, Field(strict=True, max_length=18)]] = Field(default=None, description="Work phone number. Digits only, no formatting.", alias="workPhone", json_schema_extra={"examples": ["5145550123"]})
    account_opened_date: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Account creation date. Format YYYYMMDD.", alias="accountOpenedDate", json_schema_extra={"examples": ["20240115"]})
    account_age_indicator: Optional[StrictStr] = Field(default=None, description="Account age token: 01 = created during transaction, 02 = <30 days, 03 = 30–60 days, 04 = >60 days.", alias="accountAgeIndicator", json_schema_extra={"examples": ["04"]})
    is_free_account: Optional[StrictBool] = Field(default=None, description="True if the customer account is a free (non-paying) account.", alias="isFreeAccount", json_schema_extra={"examples": [False]})
    account_last_changed_date: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Last account modification date. Format YYYYMMDD.", alias="accountLastChangedDate", json_schema_extra={"examples": ["20250101"]})
    account_change_indicator: Optional[StrictStr] = Field(default=None, description="Account change age token: 01 = during transaction, 02 = <30 days, 03 = 30–60 days, 04 = >60 days.", alias="accountChangeIndicator", json_schema_extra={"examples": ["04"]})
    password_last_changed_date: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Last password change date. Format YYYYMMDD.", alias="passwordLastChangedDate", json_schema_extra={"examples": ["20250101"]})
    password_change_indicator: Optional[StrictStr] = Field(default=None, description="Password change age: 01 = never reset, 02 = during transaction, 03 = <30 days, 04 = 30–60 days, 05 = >60 days.", alias="passwordChangeIndicator", json_schema_extra={"examples": ["01"]})
    transaction_successful_count_last_six_months: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=None, description="Count of successful purchases in the past 6 months.", alias="transactionSuccessfulCountLastSixMonths", json_schema_extra={"examples": [7]})
    transaction_attempted_count_last24_hours: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=None, description="Count of transaction attempts in the past 24 hours.", alias="transactionAttemptedCountLast24Hours", json_schema_extra={"examples": [1]})
    transaction_attempted_count_last_year: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=None, description="Count of transaction attempts in the past year.", alias="transactionAttemptedCountLastYear", json_schema_extra={"examples": [12]})
    payment_method_added_date: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Date payment method was added. Format YYYYMMDD.", alias="paymentMethodAddedDate", json_schema_extra={"examples": ["20240115"]})
    payment_method_age_indicator: Optional[StrictStr] = Field(default=None, description="Payment method age token.", alias="paymentMethodAgeIndicator", json_schema_extra={"examples": ["04"]})
    payment_method_add_attempt_count_last24_hours: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=None, description="Number of payment method add attempts in the past 24 hours.", alias="paymentMethodAddAttemptCountLast24Hours", json_schema_extra={"examples": [0]})
    is_payment_method_on_file: Optional[StrictBool] = Field(default=None, description="True if the payment method is stored on file for the customer.", alias="isPaymentMethodOnFile", json_schema_extra={"examples": [True]})
    is_account_suspicious: Optional[StrictBool] = Field(default=None, description="True if the merchant considers the account suspicious.", alias="isAccountSuspicious", json_schema_extra={"examples": [False]})
    customer_id: Optional[Annotated[str, Field(strict=True, max_length=24)]] = Field(default=None, description="Merchant's internal customer identifier.", alias="customerId", json_schema_extra={"examples": ["cust_0294b7c1"]})
    account_authentication_method: Optional[StrictStr] = Field(default=None, description="Method used to authenticate the customer for this session.", alias="accountAuthenticationMethod", json_schema_extra={"examples": ["EMAIL_VERIFIED"]})
    is_tenured_customer: Optional[StrictBool] = Field(default=None, description="True if the customer has a long-standing, established account relationship.", alias="isTenuredCustomer", json_schema_extra={"examples": [True]})
    is_email_known_to_customer: Optional[StrictBool] = Field(default=None, description="True if the email address on file is associated with a known customer account.", alias="isEmailKnownToCustomer", json_schema_extra={"examples": [True]})
    is_registered_customer: Optional[StrictStr] = Field(default=None, description="Whether the purchaser is a registered member (Y) or guest (N).", alias="isRegisteredCustomer", json_schema_extra={"examples": ["Y"]})
    is_registration_updated: Optional[StrictStr] = Field(default=None, description="Whether any registration information changed since account creation.", alias="isRegistrationUpdated", json_schema_extra={"examples": ["N"]})
    registered_account_tenure: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=None, description="Number of days the customer has been registered with the merchant.", alias="registeredAccountTenure", json_schema_extra={"examples": [720]})
    registered_name: Optional[Annotated[str, Field(strict=True, max_length=35)]] = Field(default=None, description="Customer name as registered with the merchant.", alias="registeredName", json_schema_extra={"examples": ["Jane Smith"]})
    registered_email: Optional[Annotated[str, Field(strict=True, max_length=60)]] = Field(default=None, description="Registered email address with the merchant.", alias="registeredEmail", json_schema_extra={"examples": ["jane@example.com"]})
    registered_postal_code: Optional[Annotated[str, Field(strict=True, max_length=9)]] = Field(default=None, description="Registered postal code with the merchant.", alias="registeredPostalCode", json_schema_extra={"examples": ["32121"]})
    registered_address: Optional[Annotated[str, Field(strict=True, max_length=50)]] = Field(default=None, description="Registered address with the merchant (no city/state).", alias="registeredAddress", json_schema_extra={"examples": ["400 Maple Court"]})
    registered_phone: Optional[Annotated[str, Field(strict=True, max_length=18)]] = Field(default=None, description="Registered phone number. Digits only.", alias="registeredPhone", json_schema_extra={"examples": ["5555551111"]})
    days_since_name_change: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=None, description="Days between the last registered-name change and the purchase date.", alias="daysSinceNameChange", json_schema_extra={"examples": [31]})
    days_since_email_change: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=None, description="Days between the last registered-email change and the purchase date.", alias="daysSinceEmailChange", json_schema_extra={"examples": [31]})
    days_since_password_change: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=None, description="Days between the last password change and the purchase date.", alias="daysSincePasswordChange", json_schema_extra={"examples": [31]})
    days_since_postal_code_change: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=None, description="Days between the last registered-postal-code change and the purchase date.", alias="daysSincePostalCodeChange", json_schema_extra={"examples": [31]})
    days_since_address_change: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=None, description="Days between the last registered-address change and the purchase date.", alias="daysSinceAddressChange", json_schema_extra={"examples": [31]})
    days_since_phone_change: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=None, description="Days between the last registered-phone change and the purchase date.", alias="daysSincePhoneChange", json_schema_extra={"examples": [31]})
    days_since_ship_to_name_change: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=None, description="Days between the last ship-to-name change and the purchase date.", alias="daysSinceShipToNameChange", json_schema_extra={"examples": [31]})
    customer_ani: Optional[Annotated[str, Field(strict=True, max_length=18)]] = Field(default=None, description="ANI 10-digit phone number used to place a phone order. Digits only.", alias="customerAni", json_schema_extra={"examples": ["5555551111"]})
    customer_ani_digits: Optional[Annotated[str, Field(strict=True, max_length=2)]] = Field(default=None, description="ANI Information Identifier (II) digits: e.g. cellular = 61–63, payphone = 27, toll-free = 24/25.", alias="customerAniDigits", json_schema_extra={"examples": ["27"]})
    is_email_associated_with_fraud: Optional[StrictBool] = Field(default=None, description="True if the email has been associated with confirmed/suspected fraud (distinct from isAccountSuspicious). Carrier for BofA emailAssociatedWithFraudFlag.", alias="isEmailAssociatedWithFraud", json_schema_extra={"examples": [False]})
    __properties: ClassVar[List[str]] = ["firstName", "lastName", "email", "homePhone", "mobilePhone", "workPhone", "accountOpenedDate", "accountAgeIndicator", "isFreeAccount", "accountLastChangedDate", "accountChangeIndicator", "passwordLastChangedDate", "passwordChangeIndicator", "transactionSuccessfulCountLastSixMonths", "transactionAttemptedCountLast24Hours", "transactionAttemptedCountLastYear", "paymentMethodAddedDate", "paymentMethodAgeIndicator", "paymentMethodAddAttemptCountLast24Hours", "isPaymentMethodOnFile", "isAccountSuspicious", "customerId", "accountAuthenticationMethod", "isTenuredCustomer", "isEmailKnownToCustomer", "isRegisteredCustomer", "isRegistrationUpdated", "registeredAccountTenure", "registeredName", "registeredEmail", "registeredPostalCode", "registeredAddress", "registeredPhone", "daysSinceNameChange", "daysSinceEmailChange", "daysSincePasswordChange", "daysSincePostalCodeChange", "daysSinceAddressChange", "daysSincePhoneChange", "daysSinceShipToNameChange", "customerAni", "customerAniDigits", "isEmailAssociatedWithFraud"]

    @field_validator('account_opened_date')
    def account_opened_date_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^\d{8}$", value):
            raise ValueError(r"must validate the regular expression /^\d{8}$/")
        return value

    @field_validator('account_age_indicator')
    def account_age_indicator_validate_enum(cls, value):
        """Open vocabulary (RAP ADR-SDK-023 override): accept unknown enum values.

        The spec's enums are open vocabularies (ADR-SDK-007/010) — new server-side
        values must never break deserialization on success paths, so unknown wire
        values pass through verbatim instead of raising ValueError.
        """
        return value

    @field_validator('account_last_changed_date')
    def account_last_changed_date_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^\d{8}$", value):
            raise ValueError(r"must validate the regular expression /^\d{8}$/")
        return value

    @field_validator('account_change_indicator')
    def account_change_indicator_validate_enum(cls, value):
        """Open vocabulary (RAP ADR-SDK-023 override): accept unknown enum values.

        The spec's enums are open vocabularies (ADR-SDK-007/010) — new server-side
        values must never break deserialization on success paths, so unknown wire
        values pass through verbatim instead of raising ValueError.
        """
        return value

    @field_validator('password_last_changed_date')
    def password_last_changed_date_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^\d{8}$", value):
            raise ValueError(r"must validate the regular expression /^\d{8}$/")
        return value

    @field_validator('password_change_indicator')
    def password_change_indicator_validate_enum(cls, value):
        """Open vocabulary (RAP ADR-SDK-023 override): accept unknown enum values.

        The spec's enums are open vocabularies (ADR-SDK-007/010) — new server-side
        values must never break deserialization on success paths, so unknown wire
        values pass through verbatim instead of raising ValueError.
        """
        return value

    @field_validator('payment_method_added_date')
    def payment_method_added_date_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^\d{8}$", value):
            raise ValueError(r"must validate the regular expression /^\d{8}$/")
        return value

    @field_validator('payment_method_age_indicator')
    def payment_method_age_indicator_validate_enum(cls, value):
        """Open vocabulary (RAP ADR-SDK-023 override): accept unknown enum values.

        The spec's enums are open vocabularies (ADR-SDK-007/010) — new server-side
        values must never break deserialization on success paths, so unknown wire
        values pass through verbatim instead of raising ValueError.
        """
        return value

    @field_validator('account_authentication_method')
    def account_authentication_method_validate_enum(cls, value):
        """Open vocabulary (RAP ADR-SDK-023 override): accept unknown enum values.

        The spec's enums are open vocabularies (ADR-SDK-007/010) — new server-side
        values must never break deserialization on success paths, so unknown wire
        values pass through verbatim instead of raising ValueError.
        """
        return value

    @field_validator('is_registered_customer')
    def is_registered_customer_validate_enum(cls, value):
        """Open vocabulary (RAP ADR-SDK-023 override): accept unknown enum values.

        The spec's enums are open vocabularies (ADR-SDK-007/010) — new server-side
        values must never break deserialization on success paths, so unknown wire
        values pass through verbatim instead of raising ValueError.
        """
        return value

    @field_validator('is_registration_updated')
    def is_registration_updated_validate_enum(cls, value):
        """Open vocabulary (RAP ADR-SDK-023 override): accept unknown enum values.

        The spec's enums are open vocabularies (ADR-SDK-007/010) — new server-side
        values must never break deserialization on success paths, so unknown wire
        values pass through verbatim instead of raising ValueError.
        """
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RtnDataCustomerData from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RtnDataCustomerData from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "firstName": obj.get("firstName"),
            "lastName": obj.get("lastName"),
            "email": obj.get("email"),
            "homePhone": obj.get("homePhone"),
            "mobilePhone": obj.get("mobilePhone"),
            "workPhone": obj.get("workPhone"),
            "accountOpenedDate": obj.get("accountOpenedDate"),
            "accountAgeIndicator": obj.get("accountAgeIndicator"),
            "isFreeAccount": obj.get("isFreeAccount"),
            "accountLastChangedDate": obj.get("accountLastChangedDate"),
            "accountChangeIndicator": obj.get("accountChangeIndicator"),
            "passwordLastChangedDate": obj.get("passwordLastChangedDate"),
            "passwordChangeIndicator": obj.get("passwordChangeIndicator"),
            "transactionSuccessfulCountLastSixMonths": obj.get("transactionSuccessfulCountLastSixMonths"),
            "transactionAttemptedCountLast24Hours": obj.get("transactionAttemptedCountLast24Hours"),
            "transactionAttemptedCountLastYear": obj.get("transactionAttemptedCountLastYear"),
            "paymentMethodAddedDate": obj.get("paymentMethodAddedDate"),
            "paymentMethodAgeIndicator": obj.get("paymentMethodAgeIndicator"),
            "paymentMethodAddAttemptCountLast24Hours": obj.get("paymentMethodAddAttemptCountLast24Hours"),
            "isPaymentMethodOnFile": obj.get("isPaymentMethodOnFile"),
            "isAccountSuspicious": obj.get("isAccountSuspicious"),
            "customerId": obj.get("customerId"),
            "accountAuthenticationMethod": obj.get("accountAuthenticationMethod"),
            "isTenuredCustomer": obj.get("isTenuredCustomer"),
            "isEmailKnownToCustomer": obj.get("isEmailKnownToCustomer"),
            "isRegisteredCustomer": obj.get("isRegisteredCustomer"),
            "isRegistrationUpdated": obj.get("isRegistrationUpdated"),
            "registeredAccountTenure": obj.get("registeredAccountTenure"),
            "registeredName": obj.get("registeredName"),
            "registeredEmail": obj.get("registeredEmail"),
            "registeredPostalCode": obj.get("registeredPostalCode"),
            "registeredAddress": obj.get("registeredAddress"),
            "registeredPhone": obj.get("registeredPhone"),
            "daysSinceNameChange": obj.get("daysSinceNameChange"),
            "daysSinceEmailChange": obj.get("daysSinceEmailChange"),
            "daysSincePasswordChange": obj.get("daysSincePasswordChange"),
            "daysSincePostalCodeChange": obj.get("daysSincePostalCodeChange"),
            "daysSinceAddressChange": obj.get("daysSinceAddressChange"),
            "daysSincePhoneChange": obj.get("daysSincePhoneChange"),
            "daysSinceShipToNameChange": obj.get("daysSinceShipToNameChange"),
            "customerAni": obj.get("customerAni"),
            "customerAniDigits": obj.get("customerAniDigits"),
            "isEmailAssociatedWithFraud": obj.get("isEmailAssociatedWithFraud")
        })
        return _obj


