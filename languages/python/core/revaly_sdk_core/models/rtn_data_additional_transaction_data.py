# coding: utf-8

"""
    Revaly

    Payment processing API for transaction and payment method management.  ## API Versioning  RAP supports an explicit, selectable API version so you can build against a stable, pinned contract while existing integrations keep working unchanged.  - **How to select a version:** send the `X-Api-Version` request header   (e.g. `X-Api-Version: 2.0`). The version lives in the header — request   URLs do not change. - **Default when omitted:** requests without the header (or with an   unrecognised header name) bind to the **base version `2.0`**, which is the   current contract. Existing integrations therefore continue unchanged. - **Unsupported versions:** a header naming a version that does not exist   returns **HTTP 400** with a structured error listing the supported   versions — a request is never silently bound to a different contract.   This includes an **empty or whitespace value**: if the `X-Api-Version`   header is present, it must name a supported version. Only a fully   absent header binds to the default. - **Supported versions** are advertised via the `api-supported-versions`   header on every response from the versioned API endpoints (payments,   payment methods, transactions, notify). Currently: `2.0`, `2.1`. - **Which version to use:** new integrations should pin **`2.1`**. It is   behaviourally identical to `2.0` today, and it is where future contract   refinements will land — pinning it now means you never migrate the   header. `2.0` is the frozen launch contract and remains the binding for   requests that send no version header.  ## Request tracing  Every API response — success and error alike — carries an `X-Correlation-ID` header. Send your own value (any non-empty string) and it is echoed back verbatim; omit it and the platform generates one. Quote the id when contacting support: it joins the request directly to platform telemetry. Treat it as an opaque string. 

    The version of the OpenAPI document: 2.2.1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.

    RAP SDK generated core — DO NOT EDIT (ADR-SDK-001; CI regeneration-diff enforced).
    Regenerate only via pipeline/generate.sh: spec input pinned by spec/pin.yaml
    (ADR-SDK-006), generator digest-pinned by pipeline/generator-pin.yaml (ADR-SDK-023).
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class RtnDataAdditionalTransactionData(BaseModel):
    """
    Core transaction signals supplied by the request producer. Transaction-routing fields (bin, lastFourCardNumber, purchaseAmount, purchaseCurrency, clientInitiatedDate, traceId, providerId) are derived by Revaly at submission time and must not be sent.
    """ # noqa: E501
    provider_auth_decision: Optional[StrictStr] = Field(default=None, description="Authorization decision from the producer. Required for Trusted MID flows.", alias="providerAuthDecision", json_schema_extra={"examples": ["APPROVE"]})
    provider_auth_decision_code: Optional[StrictStr] = Field(default=None, description="Provider-specific reason code for the auth decision. Required for Trusted MID flows.", alias="providerAuthDecisionCode", json_schema_extra={"examples": ["00"]})
    payment_rail: Optional[StrictStr] = Field(default=None, description="Payment routing rail.", alias="paymentRail", json_schema_extra={"examples": ["VISA"]})
    pos_entry_mode: Optional[StrictStr] = Field(default=None, description="Credential capture method at the point of sale.", alias="posEntryMode", json_schema_extra={"examples": ["ECOMMERCE"]})
    retrieval_reference_number: Optional[Annotated[str, Field(strict=True, max_length=12)]] = Field(default=None, description="Transaction retrieval reference number.", alias="retrievalReferenceNumber", json_schema_extra={"examples": ["123456789012"]})
    merchant_trust_level: Optional[StrictStr] = Field(default=None, description="Merchant trust classification assigned by the producer.", alias="merchantTrustLevel", json_schema_extra={"examples": ["TRUSTED"]})
    merchant_trust_data: Optional[Annotated[str, Field(strict=True, max_length=2048)]] = Field(default=None, description="Producer metadata encoding the basis for the trust classification.", alias="merchantTrustData", json_schema_extra={"examples": ["tool=KOUNT;riskScore=12;program=TRUSTED_AUTH;version=1"]})
    card_brand: Optional[StrictStr] = Field(default=None, description="Card product brand.", alias="cardBrand", json_schema_extra={"examples": ["VISA"]})
    message_category: Optional[StrictStr] = Field(default=None, description="Message type: 1 = Pre-Auth Approved, 2 = Pre-Auth Declined, 3 = Pre-Auth Test.", alias="messageCategory", json_schema_extra={"examples": ["1"]})
    full_pan: Optional[Annotated[str, Field(min_length=13, strict=True, max_length=19)]] = Field(default=None, description="Full card Primary Account Number (cardholder data / CHD). Optional. Forwarded to RTN in-flight only — never persisted or logged at rest by RAP. Sending this field transmits cardholder data; ensure your integration is authorized under your PCI-DSS obligations.", alias="fullPan", json_schema_extra={"examples": ["4147202000001234"]})
    dpan: Optional[Annotated[str, Field(strict=True, max_length=19)]] = Field(default=None, description="Device Primary Account Number (tokenized card number from a digital wallet).", json_schema_extra={"examples": ["4761739001010010"]})
    __properties: ClassVar[List[str]] = ["providerAuthDecision", "providerAuthDecisionCode", "paymentRail", "posEntryMode", "retrievalReferenceNumber", "merchantTrustLevel", "merchantTrustData", "cardBrand", "messageCategory", "fullPan", "dpan"]

    @field_validator('provider_auth_decision')
    def provider_auth_decision_validate_enum(cls, value):
        """Open vocabulary (RAP ADR-SDK-023 override): accept unknown enum values.

        The spec's enums are open vocabularies (ADR-SDK-007/010) — new server-side
        values must never break deserialization on success paths, so unknown wire
        values pass through verbatim instead of raising ValueError.
        """
        return value

    @field_validator('payment_rail')
    def payment_rail_validate_enum(cls, value):
        """Open vocabulary (RAP ADR-SDK-023 override): accept unknown enum values.

        The spec's enums are open vocabularies (ADR-SDK-007/010) — new server-side
        values must never break deserialization on success paths, so unknown wire
        values pass through verbatim instead of raising ValueError.
        """
        return value

    @field_validator('pos_entry_mode')
    def pos_entry_mode_validate_enum(cls, value):
        """Open vocabulary (RAP ADR-SDK-023 override): accept unknown enum values.

        The spec's enums are open vocabularies (ADR-SDK-007/010) — new server-side
        values must never break deserialization on success paths, so unknown wire
        values pass through verbatim instead of raising ValueError.
        """
        return value

    @field_validator('merchant_trust_level')
    def merchant_trust_level_validate_enum(cls, value):
        """Open vocabulary (RAP ADR-SDK-023 override): accept unknown enum values.

        The spec's enums are open vocabularies (ADR-SDK-007/010) — new server-side
        values must never break deserialization on success paths, so unknown wire
        values pass through verbatim instead of raising ValueError.
        """
        return value

    @field_validator('card_brand')
    def card_brand_validate_enum(cls, value):
        """Open vocabulary (RAP ADR-SDK-023 override): accept unknown enum values.

        The spec's enums are open vocabularies (ADR-SDK-007/010) — new server-side
        values must never break deserialization on success paths, so unknown wire
        values pass through verbatim instead of raising ValueError.
        """
        return value

    @field_validator('message_category')
    def message_category_validate_enum(cls, value):
        """Open vocabulary (RAP ADR-SDK-023 override): accept unknown enum values.

        The spec's enums are open vocabularies (ADR-SDK-007/010) — new server-side
        values must never break deserialization on success paths, so unknown wire
        values pass through verbatim instead of raising ValueError.
        """
        return value

    @field_validator('full_pan')
    def full_pan_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^\d{13,19}$", value):
            raise ValueError(r"must validate the regular expression /^\d{13,19}$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RtnDataAdditionalTransactionData from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RtnDataAdditionalTransactionData from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "providerAuthDecision": obj.get("providerAuthDecision"),
            "providerAuthDecisionCode": obj.get("providerAuthDecisionCode"),
            "paymentRail": obj.get("paymentRail"),
            "posEntryMode": obj.get("posEntryMode"),
            "retrievalReferenceNumber": obj.get("retrievalReferenceNumber"),
            "merchantTrustLevel": obj.get("merchantTrustLevel"),
            "merchantTrustData": obj.get("merchantTrustData"),
            "cardBrand": obj.get("cardBrand"),
            "messageCategory": obj.get("messageCategory"),
            "fullPan": obj.get("fullPan"),
            "dpan": obj.get("dpan")
        })
        return _obj


