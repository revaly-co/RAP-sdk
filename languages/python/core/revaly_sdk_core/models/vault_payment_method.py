# coding: utf-8

"""
    Revaly

    Payment processing API for transaction and payment method management.  ## API Versioning  RAP supports an explicit, selectable API version so you can build against a stable, pinned contract while existing integrations keep working unchanged.  - **How to select a version:** send the `X-Api-Version` request header   (e.g. `X-Api-Version: 2.0`). The version lives in the header — request   URLs do not change. - **Default when omitted:** requests without the header (or with an   unrecognised header name) bind to the **base version `2.0`**, which is the   current contract. Existing integrations therefore continue unchanged. - **Unsupported versions:** a header naming a version that does not exist   returns **HTTP 400** with a structured error listing the supported   versions — a request is never silently bound to a different contract.   This includes an **empty or whitespace value**: if the `X-Api-Version`   header is present, it must name a supported version. Only a fully   absent header binds to the default. - **Supported versions** are advertised via the `api-supported-versions`   header on every response from the versioned API endpoints (payments,   payment methods, transactions, notify). Currently: `2.0`, `2.1`. - **Which version to use:** new integrations should pin **`2.1`**. It is   behaviourally identical to `2.0` today, and it is where future contract   refinements will land — pinning it now means you never migrate the   header. `2.0` is the frozen launch contract and remains the binding for   requests that send no version header.  ## Request tracing  Every API response — success and error alike — carries an `X-Correlation-ID` header. Send your own value (any non-empty string) and it is echoed back verbatim; omit it and the platform generates one. Quote the id when contacting support: it joins the request directly to platform telemetry. Treat it as an opaque string. 

    The version of the OpenAPI document: 2.2.1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.

    RAP SDK generated core — DO NOT EDIT (ADR-SDK-001; CI regeneration-diff enforced).
    Regenerate only via pipeline/generate.sh: spec input pinned by spec/pin.yaml
    (ADR-SDK-006), generator digest-pinned by pipeline/generator-pin.yaml (ADR-SDK-023).
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class VaultPaymentMethod(BaseModel):
    """
    Vault-issued payment token details for payment processing
    """ # noqa: E501
    vault_token: Optional[Annotated[str, Field(strict=True, max_length=200)]] = Field(default=None, description="Vault-issued token (any provider) used to authorize a payment. Valid only on `/payments/charge` and `/payments/authorize` when `paymentMethodType` is `vaultToken`. Requires `paymentMethod.merchantAccountReferenceId` for gateway routing. Must not be combined with `creditCard` or `gatewayPaymentMethod`. ", alias="vaultToken", json_schema_extra={"examples": ["vt_01HX5J8QH8N1XK2D3T4B6Q7Z9C"]})
    bin: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Bank Identification Number (first 6 or 8 digits)", json_schema_extra={"examples": ["424242"]})
    last_four_digits: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Last four digits of the payment method", alias="lastFourDigits", json_schema_extra={"examples": ["4242"]})
    expiry_year: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Expiration year (YYYY)", alias="expiryYear", json_schema_extra={"examples": ["2025"]})
    expiry_month: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Expiration month (01-12)", alias="expiryMonth", json_schema_extra={"examples": ["12"]})
    __properties: ClassVar[List[str]] = ["vaultToken", "bin", "lastFourDigits", "expiryYear", "expiryMonth"]

    @field_validator('bin')
    def bin_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^(?:[0-9]{6}|[0-9]{8})$", value):
            raise ValueError(r"must validate the regular expression /^(?:[0-9]{6}|[0-9]{8})$/")
        return value

    @field_validator('last_four_digits')
    def last_four_digits_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[0-9]{4}$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]{4}$/")
        return value

    @field_validator('expiry_year')
    def expiry_year_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^20[0-9]{2}$", value):
            raise ValueError(r"must validate the regular expression /^20[0-9]{2}$/")
        return value

    @field_validator('expiry_month')
    def expiry_month_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^(0[1-9]|1[0-2])$", value):
            raise ValueError(r"must validate the regular expression /^(0[1-9]|1[0-2])$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of VaultPaymentMethod from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if vault_token (nullable) is None
        # and model_fields_set contains the field
        if self.vault_token is None and "vault_token" in self.model_fields_set:
            _dict['vaultToken'] = None

        # set to None if bin (nullable) is None
        # and model_fields_set contains the field
        if self.bin is None and "bin" in self.model_fields_set:
            _dict['bin'] = None

        # set to None if last_four_digits (nullable) is None
        # and model_fields_set contains the field
        if self.last_four_digits is None and "last_four_digits" in self.model_fields_set:
            _dict['lastFourDigits'] = None

        # set to None if expiry_year (nullable) is None
        # and model_fields_set contains the field
        if self.expiry_year is None and "expiry_year" in self.model_fields_set:
            _dict['expiryYear'] = None

        # set to None if expiry_month (nullable) is None
        # and model_fields_set contains the field
        if self.expiry_month is None and "expiry_month" in self.model_fields_set:
            _dict['expiryMonth'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of VaultPaymentMethod from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "vaultToken": obj.get("vaultToken"),
            "bin": obj.get("bin"),
            "lastFourDigits": obj.get("lastFourDigits"),
            "expiryYear": obj.get("expiryYear"),
            "expiryMonth": obj.get("expiryMonth")
        })
        return _obj


