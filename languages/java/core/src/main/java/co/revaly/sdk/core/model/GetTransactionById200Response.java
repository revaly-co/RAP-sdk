/*
 * Revaly
 * Payment processing API for transaction and payment method management.  ## API Versioning  RAP supports an explicit, selectable API version so you can build against a stable, pinned contract while existing integrations keep working unchanged.  - **How to select a version:** send the `X-Api-Version` request header   (e.g. `X-Api-Version: 2.0`). The version lives in the header — request   URLs do not change. - **Default when omitted:** requests without the header (or with an   unrecognised header name) bind to the **base version `2.0`**, which is the   current contract. Existing integrations therefore continue unchanged. - **Unsupported versions:** a header naming a version that does not exist   returns **HTTP 400** with a structured error listing the supported   versions — a request is never silently bound to a different contract.   This includes an **empty or whitespace value**: if the `X-Api-Version`   header is present, it must name a supported version. Only a fully   absent header binds to the default. - **Supported versions** are advertised via the `api-supported-versions`   header on every response from the versioned API endpoints (payments,   payment methods, transactions, notify). Currently: `2.0`, `2.1`. - **Which version to use:** new integrations should pin **`2.1`**. It is   behaviourally identical to `2.0` today, and it is where future contract   refinements will land — pinning it now means you never migrate the   header. `2.0` is the frozen launch contract and remains the binding for   requests that send no version header.  ## Request tracing  Every API response — success and error alike — carries an `X-Correlation-ID` header. Send your own value (any non-empty string) and it is echoed back verbatim; omit it and the platform generates one. Quote the id when contacting support: it joins the request directly to platform telemetry. Treat it as an opaque string. 
 *
 * The version of the OpenAPI document: 2.3.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 * RAP SDK generated core — DO NOT EDIT (ADR-SDK-001; CI regeneration-diff enforced).
 * Regenerate only via pipeline/generate.sh: spec input pinned by spec/pin.yaml
 * (ADR-SDK-006), generator digest-pinned by pipeline/generator-pin.yaml (ADR-SDK-023).
 */


package co.revaly.sdk.core.model;

import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.StringJoiner;
import java.util.Objects;
import java.util.Map;
import java.util.HashMap;
import co.revaly.sdk.core.model.PaymentMethodResponse;
import co.revaly.sdk.core.model.PaymentPlanData;
import co.revaly.sdk.core.model.Recovery;
import co.revaly.sdk.core.model.StoredCredentialResponse;
import co.revaly.sdk.core.model.TransactionGroupResponse;
import co.revaly.sdk.core.model.TransactionResponse;
import co.revaly.sdk.core.model.TransactionResponseDetails;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonValue;
import java.time.OffsetDateTime;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashMap;
import java.util.List;
import java.util.Map;
import org.openapitools.jackson.nullable.JsonNullable;
import com.fasterxml.jackson.annotation.JsonIgnore;
import org.openapitools.jackson.nullable.JsonNullable;
import java.util.NoSuchElementException;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;

import com.fasterxml.jackson.core.type.TypeReference;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.JsonMappingException;
import com.fasterxml.jackson.databind.MapperFeature;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonToken;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import co.revaly.sdk.core.ApiClient;
import co.revaly.sdk.core.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.23.0")
@JsonDeserialize(using = GetTransactionById200Response.GetTransactionById200ResponseDeserializer.class)
@JsonSerialize(using = GetTransactionById200Response.GetTransactionById200ResponseSerializer.class)
public class GetTransactionById200Response extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(GetTransactionById200Response.class.getName());

    public static class GetTransactionById200ResponseSerializer extends StdSerializer<GetTransactionById200Response> {
        public GetTransactionById200ResponseSerializer(Class<GetTransactionById200Response> t) {
            super(t);
        }

        public GetTransactionById200ResponseSerializer() {
            this(null);
        }

        @Override
        public void serialize(GetTransactionById200Response value, JsonGenerator jgen, SerializerProvider provider) throws IOException, JsonProcessingException {
            jgen.writeObject(value.getActualInstance());
        }
    }

    public static class GetTransactionById200ResponseDeserializer extends StdDeserializer<GetTransactionById200Response> {
        public GetTransactionById200ResponseDeserializer() {
            this(GetTransactionById200Response.class);
        }

        public GetTransactionById200ResponseDeserializer(Class<?> vc) {
            super(vc);
        }

        @Override
        public GetTransactionById200Response deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException, JsonProcessingException {
            JsonNode tree = ctxt.readTree(jp);
            Object deserialized = null;
            boolean typeCoercion = ctxt.isEnabled(MapperFeature.ALLOW_COERCION_OF_SCALARS);
            int match = 0;
            JsonToken token = tree.traverse(jp.getCodec()).nextToken();
            // RAP fork, pass 1 (STRICT): attempt each object branch with unknown
            // properties REJECTED. Against a server matching the pinned spec, exactly one
            // branch strict-matches, which is the correct discrimination the lenient pass
            // below cannot provide (all-optional models lenient-match everything).
            int strictMatch = 0;
            Object strictDeserialized = null;
            // strict-deserialize TransactionGroupResponse
            try {
                boolean strictAttempt = !(TransactionGroupResponse.class.equals(Integer.class) || TransactionGroupResponse.class.equals(Long.class) || TransactionGroupResponse.class.equals(Float.class) || TransactionGroupResponse.class.equals(Double.class) || TransactionGroupResponse.class.equals(Boolean.class) || TransactionGroupResponse.class.equals(String.class));
                if (strictAttempt && tree.isObject()) {
                    strictDeserialized = ((com.fasterxml.jackson.databind.ObjectMapper) jp.getCodec())
                            .readerFor(TransactionGroupResponse.class)
                            .with(com.fasterxml.jackson.databind.DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES)
                            .readValue(tree.traverse(jp.getCodec()));
                    strictMatch++;
                    log.log(Level.FINER, "Input data strictly matches schema 'TransactionGroupResponse'");
                }
            } catch (Exception e) {
                // strict mismatch, continue
                log.log(Level.FINER, "Input data does not strictly match schema 'TransactionGroupResponse'", e);
            }
            // strict-deserialize TransactionResponse
            try {
                boolean strictAttempt = !(TransactionResponse.class.equals(Integer.class) || TransactionResponse.class.equals(Long.class) || TransactionResponse.class.equals(Float.class) || TransactionResponse.class.equals(Double.class) || TransactionResponse.class.equals(Boolean.class) || TransactionResponse.class.equals(String.class));
                if (strictAttempt && tree.isObject()) {
                    strictDeserialized = ((com.fasterxml.jackson.databind.ObjectMapper) jp.getCodec())
                            .readerFor(TransactionResponse.class)
                            .with(com.fasterxml.jackson.databind.DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES)
                            .readValue(tree.traverse(jp.getCodec()));
                    strictMatch++;
                    log.log(Level.FINER, "Input data strictly matches schema 'TransactionResponse'");
                }
            } catch (Exception e) {
                // strict mismatch, continue
                log.log(Level.FINER, "Input data does not strictly match schema 'TransactionResponse'", e);
            }
            if (strictMatch == 1) {
                GetTransactionById200Response retStrict = new GetTransactionById200Response();
                retStrict.setActualInstance(strictDeserialized);
                return retStrict;
            }
            // RAP fork, pass 2 (stock lenient) collects every candidate so the coverage
            // tiebreak below can resolve multi-matches (e.g. a server newer than the
            // pinned spec sending additive fields, which fails every strict attempt).
            java.util.List<Object> lenientMatches = new ArrayList<>();
            java.util.List<Class<?>> lenientClasses = new ArrayList<>();
            // deserialize TransactionGroupResponse
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (TransactionGroupResponse.class.equals(Integer.class) || TransactionGroupResponse.class.equals(Long.class) || TransactionGroupResponse.class.equals(Float.class) || TransactionGroupResponse.class.equals(Double.class) || TransactionGroupResponse.class.equals(Boolean.class) || TransactionGroupResponse.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((TransactionGroupResponse.class.equals(Integer.class) || TransactionGroupResponse.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((TransactionGroupResponse.class.equals(Float.class) || TransactionGroupResponse.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (TransactionGroupResponse.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (TransactionGroupResponse.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(TransactionGroupResponse.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    lenientMatches.add(deserialized);
                    lenientClasses.add(TransactionGroupResponse.class);
                    log.log(Level.FINER, "Input data matches schema 'TransactionGroupResponse'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'TransactionGroupResponse'", e);
            }

            // deserialize TransactionResponse
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (TransactionResponse.class.equals(Integer.class) || TransactionResponse.class.equals(Long.class) || TransactionResponse.class.equals(Float.class) || TransactionResponse.class.equals(Double.class) || TransactionResponse.class.equals(Boolean.class) || TransactionResponse.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((TransactionResponse.class.equals(Integer.class) || TransactionResponse.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((TransactionResponse.class.equals(Float.class) || TransactionResponse.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (TransactionResponse.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (TransactionResponse.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(TransactionResponse.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    lenientMatches.add(deserialized);
                    lenientClasses.add(TransactionResponse.class);
                    log.log(Level.FINER, "Input data matches schema 'TransactionResponse'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'TransactionResponse'", e);
            }

            if (match == 1) {
                GetTransactionById200Response ret = new GetTransactionById200Response();
                ret.setActualInstance(deserialized);
                return ret;
            }
            // RAP fork, pass 3 (coverage tiebreak): among lenient multi-matches, bind the
            // branch that recognizes strictly the most top-level fields of the payload; a
            // genuine tie stays an error (stock behaviour). Field NAMES only are inspected,
            // never values.
            if (match > 1) {
                int bestIndex = -1;
                int bestCoverage = -1;
                boolean coverageTie = false;
                for (int i = 0; i < lenientMatches.size(); i++) {
                    int coverage = countRecognizedTopLevelFields(tree, lenientClasses.get(i), (com.fasterxml.jackson.databind.ObjectMapper) jp.getCodec());
                    if (coverage > bestCoverage) {
                        bestCoverage = coverage;
                        bestIndex = i;
                        coverageTie = false;
                    } else if (coverage == bestCoverage) {
                        coverageTie = true;
                    }
                }
                if (!coverageTie && bestIndex >= 0) {
                    log.log(Level.FINER, String.format(java.util.Locale.ROOT, "Ambiguous lenient match for GetTransactionById200Response resolved by field coverage to '%s'", lenientClasses.get(bestIndex).getSimpleName()));
                    GetTransactionById200Response retCoverage = new GetTransactionById200Response();
                    retCoverage.setActualInstance(lenientMatches.get(bestIndex));
                    return retCoverage;
                }
            }
            throw new IOException(String.format(java.util.Locale.ROOT, "Failed deserialization for GetTransactionById200Response: %d classes match result, expected 1", match));
        }

        // RAP fork helper: how many of the payload's top-level field names the target
        // model recognizes as Jackson-visible properties (names only; values untouched).
        private static int countRecognizedTopLevelFields(JsonNode tree, Class<?> target, com.fasterxml.jackson.databind.ObjectMapper mapper) {
            if (!tree.isObject()) {
                return 0;
            }
            java.util.Set<String> known = new HashSet<>();
            com.fasterxml.jackson.databind.BeanDescription description =
                    mapper.getDeserializationConfig().introspect(mapper.constructType(target));
            for (com.fasterxml.jackson.databind.introspect.BeanPropertyDefinition property : description.findProperties()) {
                known.add(property.getName());
            }
            int recognized = 0;
            java.util.Iterator<String> names = tree.fieldNames();
            while (names.hasNext()) {
                if (known.contains(names.next())) {
                    recognized++;
                }
            }
            return recognized;
        }

        /**
         * Handle deserialization of the 'null' value.
         */
        @Override
        public GetTransactionById200Response getNullValue(DeserializationContext ctxt) throws JsonMappingException {
            throw new JsonMappingException(ctxt.getParser(), "GetTransactionById200Response cannot be null");
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<>();

    public GetTransactionById200Response() {
        super("oneOf", Boolean.FALSE);
    }

    public GetTransactionById200Response(TransactionGroupResponse o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public GetTransactionById200Response(TransactionResponse o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("TransactionGroupResponse", TransactionGroupResponse.class);
        schemas.put("TransactionResponse", TransactionResponse.class);
        JSON.registerDescendants(GetTransactionById200Response.class, Collections.unmodifiableMap(schemas));
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return GetTransactionById200Response.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * TransactionGroupResponse, TransactionResponse
     *
     * It could be an instance of the 'oneOf' schemas.
     * The oneOf child schemas may themselves be a composed schema (allOf, anyOf, oneOf).
     */
    @Override
    public void setActualInstance(Object instance) {
        if (JSON.isInstanceOf(TransactionGroupResponse.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        if (JSON.isInstanceOf(TransactionResponse.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be TransactionGroupResponse, TransactionResponse");
    }

    /**
     * Get the actual instance, which can be the following:
     * TransactionGroupResponse, TransactionResponse
     *
     * @return The actual instance (TransactionGroupResponse, TransactionResponse)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `TransactionGroupResponse`. If the actual instance is not `TransactionGroupResponse`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `TransactionGroupResponse`
     * @throws ClassCastException if the instance is not `TransactionGroupResponse`
     */
    public TransactionGroupResponse getTransactionGroupResponse() throws ClassCastException {
        return (TransactionGroupResponse)super.getActualInstance();
    }

    /**
     * Get the actual instance of `TransactionResponse`. If the actual instance is not `TransactionResponse`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `TransactionResponse`
     * @throws ClassCastException if the instance is not `TransactionResponse`
     */
    public TransactionResponse getTransactionResponse() throws ClassCastException {
        return (TransactionResponse)super.getActualInstance();
    }



  /**
   * Convert the instance into URL query string.
   *
   * @return URL query string
   */
  public String toUrlQueryString() {
    return toUrlQueryString(null);
  }

  /**
   * Convert the instance into URL query string.
   *
   * @param prefix prefix of the query string
   * @return URL query string
   */
  public String toUrlQueryString(String prefix) {
    String suffix = "";
    String containerSuffix = "";
    String containerPrefix = "";
    if (prefix == null) {
      // style=form, explode=true, e.g. /pet?name=cat&type=manx
      prefix = "";
    } else {
      // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
      prefix = prefix + "[";
      suffix = "]";
      containerSuffix = "]";
      containerPrefix = "[";
    }

    StringJoiner joiner = new StringJoiner("&");

    if (getActualInstance() instanceof TransactionResponse) {
        if (getActualInstance() != null) {
          joiner.add(((TransactionResponse)getActualInstance()).toUrlQueryString(prefix + "one_of_0" + suffix));
        }
        return joiner.toString();
    }
    if (getActualInstance() instanceof TransactionGroupResponse) {
        if (getActualInstance() != null) {
          joiner.add(((TransactionGroupResponse)getActualInstance()).toUrlQueryString(prefix + "one_of_1" + suffix));
        }
        return joiner.toString();
    }
    return null;
  }

}

