# Pinned gated spec artifact — the ONLY spec input to the SDK pipeline (ADR-SDK-006).
# Bumped by PR only. Never point the generator at a branch checkout, a URL, or a local edit.
# Pipeline stage 1 re-verifies: download the release assets from `repo` at `releaseTag`,
# check openapi.bundled.yaml against `sha256` (and the committed .sha256 / provenance.json
# copies), then re-lint + re-bundle as defense in depth (docs/pipeline-and-release.md §2).
repo: revaly-co/Backbone
releaseTag: spec/v2.2.1+c4000e9
specVersion: 2.2.1
sourceCommit: c4000e9715bd2f6a2f2f486c338659c9c77c6fc5
assets:
  spec: openapi.bundled.yaml
  checksum: openapi.bundled.yaml.sha256
  provenance: provenance.json
sha256:
  openapi.bundled.yaml: efc78066a4724b96e6401f6b08ca32b31c1952c6746aab9143f506aa615acbdf
pinnedAt: "2026-07-16"
notes: >-
  Fourth pin (was spec/v2.2.0+1e59620). Backbone PR #247 documents the X-Correlation-ID
  response header the platform has always emitted (echoed verbatim when the caller sends one,
  generated otherwise): new components/headers/XCorrelationId referenced from all 8 shared
  error responses and every endpoint 200, plus a "Request tracing" section in the API
  description. Documentation of existing behaviour, hence the patch bump 2.2.0 -> 2.2.1.
  Closes the SDK-side spec gap flagged in rap-sdk PR #14 — the .NET runtime already surfaces
  exactly this header on every typed error (DX contract §c); the header name is now
  contract-protected (CorrelationIdContractTests in the platform suite). All four platform
  gates pass; provenance ciRunId 29529926459. Prior pin history: v2.1.2+9af661b (first pin,
  int64 maximum fix) -> v2.1.3+e75c71a (orphan PaymentMethodRequest dropped) ->
  v2.2.0+1e59620 (additive statementDescriptor, STR-108).
  Repo pointer updated 2026-07-17 for the org migration (FlexPay-io -> revaly-co transfer);
  releaseTag, checksums and provenance unchanged — same artifact, new owner slug.
