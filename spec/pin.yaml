# Pinned gated spec artifact — the ONLY spec input to the SDK pipeline (ADR-SDK-006).
# Bumped by PR only. Never point the generator at a branch checkout, a URL, or a local edit.
# Pipeline stage 1 re-verifies: download the release assets from `repo` at `releaseTag`,
# check openapi.bundled.yaml against `sha256` (and the committed .sha256 / provenance.json
# copies), then re-lint + re-bundle as defense in depth (docs/pipeline-and-release.md §2).
repo: FlexPay-io/Backbone
releaseTag: spec/v2.2.0+1e59620
specVersion: 2.2.0
sourceCommit: 1e59620a5527fe396a0fab86274afb3e0b10d0c8
assets:
  spec: openapi.bundled.yaml
  checksum: openapi.bundled.yaml.sha256
  provenance: provenance.json
sha256:
  openapi.bundled.yaml: 7e568e4d617fe52de7bc94355bc2ebfaea20f661d5ea8b7b3bbcd2bae64d5e9b
pinnedAt: "2026-07-16"
notes: >-
  Third pin (was spec/v2.1.3+e75c71a). Backbone STR-108 (PR #228) adds optional nullable
  `statementDescriptor` (string, maxLength 255) to the Revaly API 2.0 charge request and
  response schemas — merchant-supplied statement text, adapted per gateway at submission and
  never blocking the charge. Purely additive, hence the minor bump 2.1.3 -> 2.2.0; bundle diff
  is exactly info.version + the four schema insertions and their examples. All four platform
  gates pass (lint, bundle, breaking, contract suite); provenance ciRunId 29505465308.
  Prior pin history: v2.1.2+9af661b (first pin, int64 maximum fix) -> v2.1.3+e75c71a (orphan
  PaymentMethodRequest schema dropped). Evidence: docs/generator-bakeoff.md §3.1.
