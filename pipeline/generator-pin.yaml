# Generator toolchain pin — ADR-SDK-023 (OQ-1 closure, 2026-07-14).
# Checksum-pinned like any release tooling (docs/pipeline-and-release.md §6): pipeline stage 2
# runs generation from this image digest only — never a floating tag, never a local install.
# Upgrades ride PRs (an ADR-SDK-023 revision) where the stage-2 regeneration diff makes the
# blast radius reviewable.
generator: openapi-generator
version: v7.23.0
image: openapitools/openapi-generator-cli:v7.23.0
imageDigest: sha256:5ffccd3b0d4ac57eac443e1c9b3e2f2bb7f0a21ffe6c6701f3690d7edc78bf2d
pinnedAt: "2026-07-14"
# Per-language generation configs (flags + template partials) land with pipeline stage 2 under
# pipeline/<language>/ — the normative flag set is the table in ADR-SDK-023.
