# Input for the hash-pinned CI lockfile (uv pip compile — see header of python-runtime.txt).
# Mirrors languages/python/pyproject.toml [project].dependencies — keep in sync by hand.
urllib3>=2.7.0,<3.0.0
python-dateutil>=2.8.2
pydantic>=2.11
typing-extensions>=4.7.1
